# 🇷🇺 Fabric: Документация на русском языке (Неофициальный перевод)

> [!CAUTION]
> **ОТКАЗ ОТ ОТВЕТСТВЕННОСТИ / DISCLAIMER**
>
> Данный репозиторий является **неофициальным** проектом и поддерживается энтузиастом. Это не основной источник документации. Автор перевода не связан с командой разработки Fabric.

---

## 📋 О проекте

Этот репозиторий создан для помощи русскоязычному сообществу в освоении фреймворка **Fabric** — инструмента для разработки графических интерфейсов и виджетов рабочего стола на Linux (на базе Python и GTK).

### ⚠️ Важные примечания:
1. **Приоритет оригинала:** В случае любых неточностей, ошибок или расхождений в коде, всегда считайте верной [официальную англоязычную документацию]([https://fabric-development.github.io/fabric/](https://wiki.ffpy.org/getting-started/introduction/)).
2. **Техническая поддержка:** По вопросам багов в самом фреймворке или предложений по функционалу, пожалуйста, обращайтесь в основной репозиторий проекта.
3. **Актуальность:** Перевод может отставать от оригинальных обновлений. Проверяйте версию документации.

---

## 🔗 Официальные ресурсы проекта Fabric

Для получения актуальной информации и поддержки используйте следующие ссылки:

* **Original Documentation:** [fabric-development.github.io/fabric/](https://fabric-development.github.io/fabric/)
* **GitHub Repository:** [github.com/Fabric-Development/fabric](https://github.com/Fabric-Development/fabric)
* **Discord Server:** [Fabric Development Community]([https://discord.gg/fabric-development](https://discord.gg/3sDbYc9SZP))

---

## 🛠 Как помочь
Если вы нашли ошибку в переводе или хотите дополнить документацию, вы можете создать **Pull Request** или открыть **Issue** в этом репозитории. Давайте развивать Linux-сообщество вместе!

---

## 1. Введение в Fabric (Getting Started: Introduction)

**URL:** https://wiki.ffpy.org/getting-started/introduction/

### I. Общее описание и философия

**Fabric** — это фреймворк для создания виджетов, который полностью написан и конфигурируется на языке программирования Python. Он не требует использования других языков разметки или стилей (таких как CSS, XML или JSON).

**Ключевые цели и философия проекта:**

1. **Простота:** Предоставить максимально простой и интуитивно понятный способ для объявления и управления виджетами рабочего стола.
2. **Легковесность:** Быть ресурсоэффективным решением.
3. **Высокоуровневый API:** Абстрагировать сложные низкоуровневые детали, предоставляя разработчику удобный и мощный инструментарий.

**Поток управления на основе сигналов (Signal-Flow):**

* Это ключевая архитектурная особенность Fabric.
* Такой подход значительно упрощает обработку событий (например, кликов мыши, изменений состояния) и управление состоянием виджетов.
* Вместо сложной вложенной логики обратных вызовов, Fabric позволяет декларативно связывать события с действиями.

**Проекты-вдохновители:**

* Fabric был создан под влиянием таких популярных инструментов для кастомизации рабочего стола, как:
  * **eww (Elkowars Wacky Widgets):** Известен своим декларативным подходом к созданию виджетов.
  * **ags (Aylur's Gtk Shell):** Использует JavaScript для создания виджетов на базе Gtk.
  * **Waybar:** Популярная и легко настраиваемая панель для композиторов Wayland.
* Цель Fabric — взять лучшие идеи из этих проектов и предоставить знакомый, но улучшенный опыт для пользователей.

### II. Преимущества использования Fabric

* **Низкий порог входа для Python-разработчиков:** Если вы уже знакомы с Python, вам не придется изучать новый язык или синтаксис.
* **Простота настройки:** Может стать отличной альтернативой для тех, у кого возникают трудности с более сложными в настройке инструментами.
* **Отличная интеграция с IDE:**
  * **Строгая типизация (Type Hinting):** Весь код Fabric снабжен аннотациями типов, что позволяет статическим анализаторам и редакторам кода (VSCode, PyCharm и др.) предоставлять точное автодополнение.
  * **Документационные строки (Docstrings):** Каждый модуль, класс и функция подробно документированы, что позволяет получать подсказки и документацию прямо в редакторе.
* **Мощь экосистемы Python:** Так как Fabric является стандартной библиотекой Python, вы можете без ограничений использовать любые другие пакеты и библиотеки из огромной экосистемы Python (например, для работы с сетью, файлами, API и т.д.) прямо в коде ваших виджетов.

### III. Технологическая основа

Fabric не существует в вакууме, а построен на проверенных и мощных технологиях с открытым исходным кодом.

* **GTK 3 (GIMP ToolKit):**
  * Это основа всего фреймворка. GTK — это кроссплатформенный набор инструментов для создания графических пользовательских интерфейсов.
  * Fabric использует именно 3-ю версию GTK.
* **PyGObject (Python GObject Introspection):**
  * Это мост между Python и нативными библиотеками, написанными на C/C++ и использующими систему GObject (как GTK).
  * PyGObject позволяет Python-коду напрямую вызывать функции GTK, создавать классы виджетов и взаимодействовать с основной системой отрисовки.
* **Cairo:**
  * Это мощная 2D-графическая библиотека, которую GTK использует для отрисовки всего, что вы видите на экране.
  * GTK предоставляет возможность создавать кастомные виджеты, рисуя их с помощью Cairo. Fabric активно использует эту возможность, чтобы реализовывать собственные, уникальные виджеты, которых нет в стандартной библиотеке GTK.

Таким образом, Fabric — это не замена GTK, а его высокоуровневое расширение и обертка, написанная на Python для упрощения и ускорения разработки виджетов.

---

## 2. Подготовка к работе (Getting Started: Prepare)

**URL:** https://wiki.ffpy.org/getting-started/prepare/

### I. Необходимые знания Python

Документация подчеркивает, что для эффективного использования всей мощи Fabric требуется уверенное владение основами Python.

**Ключевые концепции Python, которые нужно знать:**

* **Пакетный менеджмент:** Умение устанавливать пакеты через `pip` и работать с виртуальными окружениями (например, `venv`) для изоляции зависимостей проекта.
* **Основы языка:**
  * Переменные и встроенные типы данных (строки, числа, списки, словари и т.д.).
  * Функции: Понимание синтаксиса `def` и анонимных `lambda`-функций.
  * Управляющие конструкции: Условные операторы (`if/elif/else`).
  * Циклы: `for` и `while`.
  * Обработка исключений: Механизм `try...except...finally` для корректной обработки ошибок.
* **Объектно-ориентированное программирование (ООП):** Понимание того, что такое классы, объекты, наследование и как они работают в Python.

**Рекомендуемые ресурсы для изучения Python:**

* **[Официальный туториал Python](https://docs.python.org/3/tutorial/)**: Полное и авторитетное руководство.
* **[Automate the Boring Stuff with Python](https://automatetheboringstuff.com/)**: Практически-ориентированная книга для начинающих, доступная бесплатно.
* **[Learn X in Y Minutes (Python 3)](https://learnxinyminutes.com/docs/python3/)**: Быстрый и сжатый справочник для тех, кто уже имеет опыт в программировании.
* **[A Byte of Python](http://python.swaroopch.com/)**: Еще одна подробная и бесплатная книга.
* **[CodeAbbey](http://www.codeabbey.com/)**: Сайт с упражнениями для отработки практических навыков программирования.

### II. Знания GTK (для продвинутых пользователей)

* Для базового использования Fabric (создание виджетов из готовых блоков) глубокие знания GTK не требуются.
* Однако, если вы планируете **расширять** функциональность Fabric, **создавать собственные сложные виджеты** с нуля или **вносить вклад в разработку** самого фреймворка, рабочее понимание GTK становится **обязательным**.
* Ресурсы для изучения GTK можно найти в "Руководстве по хакингу" (Hacking Guide), на которое дана ссылка.

### III. Следующие шаги

После ознакомления с необходимыми предпосылками, документация предлагает перейти к **[Руководству по установке](https://wiki.ffpy.org/getting-started/installation-guide/)**, а затем — к написанию **[первого виджета](https://wiki.ffpy.org/guide/first-widget/)**.

---

## 3. Руководство по установке (Getting Started: Installation Guide)

**URL:** https://wiki.ffpy.org/getting-started/installation-guide/

### I. Системные требования

* **Python:** Требуется версия **3.11** или новее.

### II. Установка Python

Проверьте инструкции вашего дистрибутива или менеджер пакетов, чтобы установить необходимую версию.

* **Arch Linux:** `sudo pacman -S python`
* **OpenSUSE:** `sudo zypper install python311`
* **Fedora:** `sudo dnf in python` (если еще не установлен)

### III. Установка зависимостей

Fabric требует для своей работы ряд системных библиотек и пакетов.

**Общий список зависимостей:**

* `gtk3`
* `cairo`
* `gtk-layer-shell`
* `libgirepository`
* `gobject-introspection`
* `gobject-introspection-runtime`
* `python`
* `python-pip`
* `python-gobject`
* `python-cairo`
* `python-loguru`
* `pkgconf`

**Примечание:** Названия пакетов и инструкции по установке могут различаться в зависимости от дистрибутива и системы. Возможно, вам придется провести небольшое исследование для вашей системы.

**Команды для популярных дистрибутивов:**

* **Arch Linux:**

  ```bash
  sudo pacman -S gtk3 cairo gtk-layer-shell libgirepository gobject-introspection gobject-introspection-runtime python python-pip python-gobject python-cairo python-loguru pkgconf
  ```
* **Ubuntu 24.04:**

  ```bash
  sudo apt-get install build-essential pkg-config python3-cairo-dev python3-dev python-is-python3 python-dev-is-python3 cairo2-dev gobject-introspection libgirepository-1.0-dev
  ```
* **openSUSE (проверено на Tumbleweed):**

  ```bash
  sudo zypper install gtk3-devel cairo-devel gtk-layer-shell-devel libgirepository-1_0-1 libgirepository-2_0-0 gobject-introspection-devel python311 python311-pip python311-gobject python311-gobject-cairo python311-pycairo python311-loguru pkgconf
  ```
* **Fedora (проверено на Fedora 42):**

  ```bash
  sudo dnf install gtk3-devel cairo-devel gtk-layer-shell-devel glib2 gobject-introspection-devel python3-devel python-pip python3-gobject python3-cairo python3-loguru pkgconf
  ```

### IV. Установка Fabric

Настоятельно рекомендуется использовать **виртуальное окружение** для установки Fabric, чтобы избежать конфликтов с системными пакетами.

#### 1. Установка в виртуальном окружении (рекомендуемый способ)

1. **Создайте и перейдите в папку проекта:**

   ```bash
   mkdir my-fabric-project && cd my-fabric-project
   ```
2. **Создайте виртуальное окружение:**

   ```bash
   python -m venv venv
   ```
3. **Активируйте окружение:**

   ```bash
   source venv/bin/activate
   ```

   *(После активации, ваша командная строка изменится, показывая, что вы работаете внутри `venv`)*
4. **Установите Fabric:**

   ```bash
   pip install git+https://github.com/Fabric-Development/fabric.git
   ```

#### 2. Глобальная установка (не рекомендуется)

Этот метод может привести к проблемам с зависимостями и не является предпочтительным.

* **Arch Linux:** Пакет `python-fabric-git` доступен в [AUR](https://aur.archlinux.org/).
* **NixOS:** Fabric можно установить через [Nix Flake](https://nixos.wiki/wiki/Flakes).
* **Другие дистрибутивы:**
  ```bash
  pip install git+https://github.com/Fabric-Development/fabric.git
  ```

### V. Следующий шаг: Заглушки (Stubs)

После установки Fabric, следующим шагом является настройка **[заглушек (stubs)](https://wiki.ffpy.org/getting-started/stubs-guide/)**. Это необходимо для корректной работы автодополнения и проверки типов в вашем редакторе кода.

---

## 4. Руководство по заглушкам (Getting Started: Stubs Guide)

**URL:** https://wiki.ffpy.org/getting-started/stubs-guide/

### I. Что такое "заглушки" (Stubs) и зачем они нужны?

"Заглушки" (или stub-файлы, `.pyi`) — это файлы, которые содержат только **определения типов** (сигнатуры функций, атрибуты классов, типы переменных) для Python-кода, но не содержат его реальной логики или исполнения.

**Основная цель:** Улучшить "Developer Experience" (опыт разработки) при работе с библиотеками, которые используют `GObject Introspection` (как Fabric/GTK), поскольку стандартные инструменты Python не всегда могут корректно "видеть" типы из этих нативных библиотек.

**Преимущества использования заглушек:**

* **Качественное автодополнение (Code Completion):** Ваш редактор кода (VSCode, PyCharm) сможет точно подсказывать доступные методы и атрибуты для объектов Fabric и GTK.
* **Проверка типов (Type Checking):** Инструменты вроде `mypy` или встроенные в IDE анализаторы смогут проверять ваш код на соответствие типов, помогая отлавливать ошибки на раннем этапе.
* **Встроенная документация:** Улучшается доступ к документационным строкам (docstrings) прямо в редакторе.

### II. Варианты пакетов заглушек

Документация предлагает несколько способов получить заглушки для PyGObject.

#### 1. GenGir (Рекомендуемый автором вариант)

* **Описание:** Это инструмент, который **генерирует** `.pyi` файлы-заглушки на основе установленных в системе библиотек GObject introspection.
* **Процесс:** Он сканирует `.gir` и `.typelib` файлы, создавая на их основе точные Python-заглушки.
* **Преимущество:** Создает наиболее полные и точные заглушки, так как они генерируются непосредственно из исходных данных вашей системы.

#### 2. FakeGir

* **Описание:** Это Python-скрипт, который генерирует один "файл переопределения" (overrides file).
* **Недостаток:** Это не полноценный пакет заглушек. Он требует ручной настройки редактора, чтобы заставить его использовать этот сгенерированный файл вместо стандартных (и часто неполных) подсказок. Считается менее удобным решением.

#### 3. PyGObject-Stubs

* **Описание:** Готовый `pip` пакет, который можно установить.
* **Недостаток:** Качество заглушек может быть неидеальным. Автор отмечает, что не все `gi` репозитории имеют аннотации или документацию, поэтому эти заглушки могут быть неполными.
* **Когда использовать:** Может быть достаточно, если вы планируете строго следовать официальной документации GTK и Fabric без глубокой кастомизации.

#### 4. Fabric Stubs (В разработке)

* **Описание:** Официальный, но пока не выпущенный пакет заглушек специально для Fabric.
* **Цель:** Будет основан на улучшенной версии заглушек, сгенерированных с помощью `GenGir`. На момент написания документации он еще не доступен для публичного использования.

### III. Рекомендации по установке

* Документация не дает прямых команд установки для каждого варианта, а отсылает к репозиториям и инструкциям каждого из этих инструментов.
* **Основная рекомендация** — использовать **GenGir** как наиболее надежный и полный способ генерации заглушек для вашей среды.

### IV. Конфигурация редактора

После установки или генерации заглушек, может потребоваться настройка вашего редактора кода, чтобы он "подхватил" их. Обычно современные редакторы с поддержкой Python (при наличии соответствующих плагинов) делают это автоматически, если заглушки установлены в активное виртуальное окружение. Для `FakeGir` требуется ручная настройка.

---

## 5. Клиент и CLI (Getting Started: Client and CLI)

**URL:** https://wiki.ffpy.org/getting-started/client-and-cli/

### I. Обзор и назначение

Fabric предоставляет утилиту командной строки (CLI) для взаимодействия с уже запущенными экземплярами ваших виджетов.

**Как это работает:**

1. Когда вы запускаете свое приложение Fabric (с помощью `Application.run()`), оно не только отображает виджеты, но и параллельно запускает **клиент D-Bus**.
2. **D-Bus** — это система межпроцессного взаимодействия (IPC), которая позволяет разным программам общаться друг с другом.
3. CLI-утилита `fabric` использует D-Bus для отправки команд запущенному приложению.

**Основное преимущество:**
Это позволяет вам "на лету" (runtime) управлять виджетами, выполнять код, изменять их состояние или получать отладочную информацию, не перезапуская основное приложение. Fabric автоматически предоставляет доступ ко всем переменным, объектам и модулям из вашего запущенного скрипта.

### II. Альтернативный CLI на Go (Рекомендуется)

* Существует **альтернативная, более быстрая и функциональная реализация CLI**, написанная на языке Go.
* Она рекомендуется к использованию вместо стандартной.
* **Ссылка:** [https://github.com/Fabric-Development/fabric-cli](https://github.com/Fabric-Development/fabric-cli)
* Встроенный в Fabric CLI на Python предоставляет лишь базовую функциональность.

### III. Использование встроенного CLI

Синтаксис запуска:

```bash
python -m fabric [OPTIONS] COMMAND [ARGS]...
```

**Опции:**

* `--help`: Показать справочное сообщение.
* `-j`, `--json`: Вывести результат в формате JSON (удобно для скриптов).

**Команды:**

#### `list-all`

Перечисляет все запущенные в данный момент экземпляры Fabric.

* **Пример:**

  ```bash
  $ python -m fabric list-all
  my-bar: /home/user/.config/fabric/config.py
  ```

  Здесь `my-bar` — это имя экземпляра, а далее указан путь к его конфигурационному файлу.
* **Пример с JSON:**

  ```bash
  $ python -m fabric list-all -j
  {"instances-dbus-names": ["org.Fabric.fabric.my-bar"]}
  ```

#### `execute <имя-экземпляра> <код>`

Выполняет строку кода Python в контексте указанного экземпляра Fabric. **Не возвращает результат** выполнения.

* **Пример (спрятать виджет `bar`):**
  ```bash
  python -m fabric execute my-bar "bar.hide()"
  ```

#### `evaluate <имя-экземпляра> <код>`

Выполняет строку кода Python и **возвращает (печатает) результат**.

* **Пример (получить значение свойства):**

  ```bash
  $ python -m fabric evaluate my-bar "bar.get_visible()"
  True
  ```
* **Пример обработки ошибки:**
  При попытке выполнить некорректный код CLI вернет исключение.

  ```bash
  $ python -m fabric evaluate my-bar "bar.this_method_does_not_exist()"
  exception: AttributeError("'StatusBar' object has no attribute 'this_method_does_not_exist'")
  ```

---

## 6. Часто задаваемые вопросы (FAQ)

**URL:** https://wiki.ffpy.org/getting-started/faq/

### I. Общие вопросы и отладка

#### Ошибки импорта в кастомных виртуальных окружениях

* **Проблема:** Если вы используете менеджер виртуальных окружений, отличный от стандартного `venv` (входящего в Python), и сталкиваетесь с ошибками импорта, несмотря на установленные зависимости.
* **Решение:** Попробуйте создать и использовать виртуальное окружение с помощью стандартной команды: `python -m venv venv`. Это может решить проблемы с путями и видимостью пакетов.

#### Не работает аудиосервис или аудиовиджет

* **Причина:** Скорее всего, отсутствует зависимость `libcvc`.
* **Решение:** Установите системный пакет `cinnamon-desktop`.
  * **Важно:** Не путайте с пакетом `cinnamon`, который является полноценной средой рабочего стола.
  * Если `cinnamon-desktop` отсутствует в вашем дистрибутиве, вы можете использовать специальный скрипт для его установки, который находится в папке `scripts/libcvc` в Git-репозитории Fabric.

#### Окно Fabric не отображается

* **Решение 1: Проверьте на блокирующий код.** Убедитесь, что перед запуском главного цикла Fabric (`Application.run()`) у вас нет кода, который надолго "зависает" (например, синхронные сетевые запросы).
* **Решение 2: Проверьте видимость.** Убедитесь, что вы явно показываете окно и его содержимое, используя метод `.show_all()` у окна или устанавливая свойство `all_visible=True`.

### II. Проблемы, специфичные для Wayland

#### Окно на мгновение меняет размер

* **Проблема:** При запуске окно может кратковременно отобразиться с неправильным размером, а затем вернуться к норме.
* **Причина:** Это известная проблема композиторов на базе `wlroots` (к которым относится, например, Sway) при отображении пустого окна `gtk-layer-shell`.
* **Обходной путь:** Не показывайте окно, пока оно пустое. Установите его свойство `visible` в `False`, добавьте в него дочерние виджеты и только потом вызовите `.show_all()`.

#### Окно отображается как обычное, а не как панель/слой

* **Причина 1: Неправильный GDK Backend.** Переменная окружения `GDK_BACKEND` может быть установлена в `x11` вместо `wayland`.
  * **Решение:** Запускайте скрипт с правильной переменной: `env GDK_BACKEND=wayland python config.py`.
* **Причина 2: Композитор не поддерживает `layer-shell`.** Некоторые среды, в частности GNOME, принципиально не поддерживают протокол `wlr-layer-shell`, необходимый для создания панелей (см. [обсуждение проблемы](https://gitlab.gnome.org/GNOME/gnome-shell/-/issues/1141#note_482766)).
* **Причина 3: Неправильный импорт.** Вы могли импортировать не тот класс окна.
  * **Неправильно:** `from fabric.widgets.window import Window`
  * **Правильно для Wayland:** `from fabric.widgets.wayland import WaylandWindow as Window`

#### Некорректно работает `EventBox` (виджет для отслеживания событий)

* **Проблема:** Нажатия мыши и другие события на `EventBox` не регистрируются.
* **Причина (для Hyprland):** Это известная проблема в Hyprland, связанная с обработкой событий на слоях. Последней версией Hyprland, где это работало корректно, была `0.28.0`.
* **Решение:** Если вы не используете Hyprland, но столкнулись с проблемой, рекомендуется создать issue на GitHub или обратиться за помощью в Discord-сообщество Fabric.

#### Нет размытия фона в Hyprland

* **Решение:** Добавьте следующие правила в ваш `hyprland.conf`:
  ```
  layerrule = blur, fabric.*
  layerrule = ignorezero, fabric.*
  ```

### III. Проблемы, специфичные для X11/Xorg

**Важное замечание:** Поддержка X11 в Fabric все еще находится на **экспериментальной** стадии, возможны ошибки.

#### Не получается добавить внешние отступы (margin) к окну

* **Решение:** Попробуйте установить тип окна на `"popup"`.

#### У окна есть закругленные углы, хотя я их не задавал

* **Причина:** Вероятно, ваш оконный композитор (например, `picom`) принудительно применяет закругление ко всем окнам.
* **Решение:** Проверьте конфигурацию вашего композитора.

#### Прозрачность не работает (черный фон вместо прозрачного)

* **Причина:** У вас не запущен композитор. Для работы прозрачности в X11 необходим композитный менеджер.
* **Решение:** Установите и запустите `picom` (рекомендуется) или `compton` (устарел).

---

## 7. Примеры (Getting Started: Examples)

**URL:** https://wiki.ffpy.org/getting-started/examples/

На этой странице собраны ссылки на готовые примеры конфигураций, демонстрирующие различные возможности Fabric. Каждый пример представляет собой полноценный мини-проект.

*(Примечание: внешний вид иконок в примерах зависит от вашей текущей темы иконок).*

### 1. Простая панель состояния (Simple Bar)

* **Описание:** Базовая реализация панели состояния (статус-бара). Отличный стартовый шаблон для создания собственной панели, особенно для Hyprland, но легко адаптируется для других оконных менеджеров Wayland и X11.
* **Ссылка на код:** [https://github.com/Fabric-Development/fabric/tree/main/examples/bar](https://github.com/Fabric-Development/fabric/tree/main/examples/bar)

![Простая панель](https://wiki.ffpy.org/assets/examples/bar.webp)

### 2. Демон уведомлений (Notifications Daemon)

* **Описание:** Пример, который показывает, как взаимодействовать с сервисом `Notifications` для создания всплывающих уведомлений. Идеальная основа для тех, кто хочет сделать собственный центр уведомлений.
* **Ссылка на код:** [https://github.com/Fabric-Development/fabric/tree/main/examples/notifications](https://github.com/Fabric-Development/fabric/tree/main/examples/notifications)

![Демон уведомлений](https://wiki.ffpy.org/assets/examples/notifications.webp)

### 3. Меню запуска приложений (Application Launcher)

* **Описание:** Демонстрирует правильный подход к созданию меню для быстрого поиска и запуска приложений, установленных в системе.
* **Ссылка на код:** [https://github.com/Fabric-Development/fabric/tree/main/examples/app-launcher](https://github.com/Fabric-Development/fabric/tree/main/examples/app-launcher)

![Меню запуска приложений](https://wiki.ffpy.org/assets/examples/app-launcher.webp)

### 4. Боковая панель (Side Panel)

* **Описание:** Пример создания многофункциональной боковой панели.
* **Ссылка на код:** [https://github.com/Fabric-Development/fabric/tree/main/examples/side-panel](https://github.com/Fabric-Development/fabric/tree/main/examples/side-panel)

![Боковая панель](https://wiki.ffpy.org/assets/examples/side-panel.webp)

### 5. Браузер иконок (Icon Browser)

* **Описание:** Полезная утилита, которая сканирует и отображает все иконки из текущей темы. При наведении курсора на иконку показывается её имя, что удобно для использования в своих конфигурациях.
* **Ссылка на код:** [https://github.com/Fabric-Development/fabric/tree/main/examples/icon-browser](https://github.com/Fabric-Development/fabric/tree/main/examples/icon-browser)

![Браузер иконок](https://wiki.ffpy.org/assets/examples/icon-browser.webp)

### 6. Менеджер Bluetooth (Bluetooth Discovery)

* **Описание:** Полноценный менеджер Bluetooth-устройств, созданный полностью на Fabric. Использует встроенный сервис `Bluetooth` для поиска устройств, сопряжения и управления подключениями.
* **Ссылка на код:** [https://github.com/Fabric-Development/fabric/tree/main/examples/bluetooth-widget](https://github.com/Fabric-Development/fabric/tree/main/examples/bluetooth-widget)

![Менеджер Bluetooth](https://wiki.ffpy.org/assets/examples/bluetooth.webp)

---

## 8. Создание первого виджета (Guide: First Widget)

**URL:** https://wiki.ffpy.org/guide/first-widget/

Это пошаговое руководство по созданию виджетов в Fabric, от самых основ до построения простой панели состояния.

### I. Основы: "Hello, World"

Вот пример использования Fabric для определения простого виджета.

```python
import fabric
from fabric import Application
from fabric.widgets.label import Label
from fabric.widgets.window import Window

# Создаем окно, передавая ему дочерний виджет Label
window = Window(
    child=Label("Hello, World"),
    all_visible=True  # Делаем видимым окно и все его дочерние элементы
)

# Инициализируем и запускаем приложение
app = Application("default", window)
app.run()
```

Запустите код с помощью `python path/to/your/file.py`. Если все в порядке, вы должны увидеть обычное окно:

![Пример с Label](https://wiki.ffpy.org/assets/first-widget-label.png)

Этот код импортирует Fabric и необходимые виджеты: `Window` и `Label`. Он создает новый экземпляр каждого виджета, причем `Label` инициализируется с текстом "Hello, World". Затем он добавляет метку в окно, делает видимым окно и всех его потомков и запускает главный цикл Fabric, чтобы окно было отрисовано.

### II. Уровень 1: Контейнеры `Box`

Теперь продемонстрируем работу с контейнерами `Box`.

```python
import fabric
from fabric import Application
from fabric.widgets.box import Box
from fabric.widgets.label import Label
from fabric.widgets.window import Window

if __name__ == "__main__":
    box_1 = Box(
        orientation="v",  # Вертикальная ориентация
        children=Label(label="это первый box")
    )
    box_2 = Box(
        spacing=28,
        orientation="h",  # Горизонтальная ориентация
        children=[
            Label(label="первый элемент второго box"),
            Label(label="элементы этого box добавляются горизонтально")
        ]
    )
    box_1.add(box_2) # Добавляем box_2 внутрь box_1

    window = Window(child=box_1, all_visible=True)
    app = Application("default", window)
    app.run()
```

Результат (обратите внимание на выравнивание):

![Пример с Box](https://wiki.ffpy.org/assets/first-widget-box.png)

Для большей ясности, вот визуальное представление:

![Схема Box](https://wiki.ffpy.org/assets/first-widget-box-help.png)

Теперь вы должны понимать, как контейнеры `Box` могут работать с разными ориентациями.

### III. Уровень 2: Интерактивность с `Button`

```python
import fabric
from fabric import Application
from fabric.widgets.box import Box
from fabric.widgets.label import Label
from fabric.widgets.button import Button
from fabric.widgets.window import Window

def create_button(): # Определяем "фабричную функцию"
    return Button(label="Нажми меня", on_clicked=lambda b, *_: b.set_label("Меня нажали!"))

if __name__ == "__main__":
    box = Box(
        orientation="v",
        spacing=10,
        children=[
            Label(label="Демо кнопок Fabric"),
            Box(
                orientation="h",
                spacing=10,
                children=[
                    create_button(),
                    create_button(),
                    create_button(),
                ],
            ),
        ],
    )
    window = Window(child=box, all_visible=True)
    app = Application("default", window)
    app.run()
```

Результат:

![Пример с Button](https://wiki.ffpy.org/assets/first-widget-button.png)

Этот пример создает окно с вертикальным `Box`. Его первый дочерний элемент — это `Label`, а второй — еще один `Box`, на этот раз горизонтальный. Этот второй `Box` содержит несколько кнопок, каждая из которых создается с помощью функции-фабрики. Поскольку один и тот же экземпляр виджета не может быть дочерним элементом дважды, функция `create_button` каждый раз создает совершенно новые экземпляры кнопок. Сигнал `on_clicked` каждой кнопки соединен с анонимной функцией, которая изменяет ее текст при нажатии.

> **Примечание:** Когда вы пишете виджет с помощью Fabric, на самом деле вы создаете под капотом полноценный виджет GTK! Цель Fabric — упростить и добавить приятные штрихи в процесс создания виджетов. Этот подход к определению виджетов покажется удивительно знакомым веб-разработчикам и тем, кто имеет опыт написания HTML.

### IV. Уровень 3: Создание простой панели состояния

Теперь, когда вы знакомы с использованием кнопок и контейнеров, мы применим эти концепции для создания базовой панели состояния.

#### 1. Специализированные окна (`WaylandWindow` и `X11Window`)

Для создания панелей, которые интегрируются с рабочим столом, нужно использовать специальные классы окон вместо обычного `Window`.

* **Обычное `Window`:** Это типичное окно, которым может управлять ваш оконный менеджер (перемещать, закрывать и т.д.).
* ***Слоевое* `Window` (`WaylandWindow` и `X11Window`):** Превращает стандартное окно приложения в виджет рабочего стола. Для каждого протокола отображения есть свой тип окна.

Замените предыдущий импорт `Window` на один из следующих:

* **Для Wayland:**
  ```python
  from fabric.widgets.wayland import WaylandWindow as Window
  ```
* **Для X11:**
  ```python
  from fabric.widgets.x11 import X11Window as Window
  ```

#### 2. Определение класса панели

Для организации кода мы создадим класс `StatusBar`, который наследуется от соответствующего класса `Window`.

* **Для Wayland:**

  ```python
  class StatusBar(Window):
      def __init__(self, **kwargs):
          super().__init__(
              layer="top",            # Расположить панель поверх обычных окон
              anchor="left top right",# "Приклеить" к верхнему краю, растянув по ширине
              exclusivity="auto",     # Зарезервировать место, чтобы другие окна не заезжали на панель
              **kwargs,
          )
  ```

  * `layer`: Гарантирует, что ваша панель останется видимой над обычными приложениями (кроме полноэкранных). Используйте `overlay` для отображения поверх всего. Для пользователей Hyprland: `hyprctl layers` покажет список слоев.
  * `anchor` (только для Wayland): Определяет положение панели. `left top right` растягивает ее по верхнему краю.
  * `exclusivity` (только для Wayland): Гарантирует, что панель получит свое собственное выделенное пространство.
* **Для X11:**

  ```python
  class StatusBar(Window):
      def __init__(self, **kwargs):
          super().__init__(
              layer="top",
              geometry="top",   # Разместить сверху
              type_hint="dock", # Сообщить оконному менеджеру, что это "док"
              **kwargs,
          )
  ```

  * `layer`: Гарантирует, что ваша панель останется видимой над обычными приложениями.
  * `geometry` и `type_hint` (только для X11): `geometry="top"` размещает панель вверху, а `type_hint="dock"` сообщает WM, что ее следует рассматривать как док-панель.

#### 3. Запуск панели и добавление виджетов

Теперь добавим содержимое в нашу панель. Мы будем использовать `CenterBox` для управления размещением элементов и `DateTime` для отображения времени.

```python
# ... (импорты и класс StatusBar)

class StatusBar(Window): # Используйте правильный родительский класс
    def __init__(self, **kwargs):
        super().__init__(
            # ... (аргументы для Wayland или X11)
            **kwargs
        )
        self.date_time = DateTime(format_string="%A, %d %B | %H:%M")
        self.children = CenterBox(center_children=self.date_time)

if __name__ == "__main__":
    bar = StatusBar(all_visible=True)
    app = Application("bar-example", bar)
    app.run()
```

С этой настройкой виджет даты и времени будет аккуратно отцентрирован на панели. Сначала панель может быть невидимой (так как она пуста и не стилизована), но вы можете заметить, что она уже зарезервировала место на экране.

#### 4. Финальный код

Вот полный код для создания простой панели состояния.

* **Wayland:**
  ```python
  import fabric
  from fabric import Application
  from fabric.widgets.datetime import DateTime
  from fabric.widgets.centerbox import CenterBox
  from fabric.widgets.wayland import WaylandWindow as Window

  class StatusBar(Window):
      def __init__(self, **kwargs):
          super().__init__(
              layer="top",
              anchor="left top right",
              exclusivity="auto",
              **kwargs
          )
          self.date_time = DateTime(format_string="%A, %d %B | %H:%M")
          self.children = CenterBox(center_children=self.date_time)

  if __name__ == "__main__":
      bar = StatusBar(all_visible=True)
      app = Application("bar-example", bar)
      app.run()
  ```
* **X11:**
  ```python
  import fabric
  from fabric import Application
  from fabric.widgets.datetime import DateTime
  from fabric.widgets.centerbox import CenterBox
  from fabric.widgets.x11 import X11Window as Window

  class StatusBar(Window):
      def __init__(self, **kwargs):
          super().__init__(
              layer="top",
              geometry="top",
              type_hint="dock",
              **kwargs
          )
          self.date_time = DateTime(format_string="%A, %d %B | %H:%M")
          self.children = CenterBox(center_children=self.date_time)

  if __name__ == "__main__":
      bar = StatusBar(all_visible=True)
      app = Application("bar-example", bar)
      app.run()
  ```

### V. Заключение

Поздравляем! 🎉 Вы только что создали свою первую базовую панель состояния с помощью Fabric. В итоге у вас должно получиться что-то вроде этого:

![Пример панели состояния](https://wiki.ffpy.org/assets/first-widget-bar.png)

Для более продвинутого примера панели, ознакомьтесь с [этим примером](https://github.com/Fabric-Development/fabric/tree/main/examples/bar).

---

## 9. Сервисы (Guide: Services)

**URL:** https://wiki.ffpy.org/guide/services/

*(Примечание: Эта страница находится в стадии разработки, некоторые части могут быть неполными или отсутствовать!)*

Сервисы (также именуемые `GObject.Object`) — это классы Python, предназначенные для выполнения определенных задач. Каждый сервис инкапсулирует отдельную функциональность и может иметь несколько *аксессуаров*: **свойства** и **сигналы**, которые позволяют сервису предоставлять свое состояние и уведомлять пользователей о важных событиях.

### I. Основные элементы сервиса

1. **Сервис (Service):** Представляет основную функциональность. Например, сервис `Fabricator` опрашивает источник данных (функцию Python или команду оболочки) через заданный интервал и управляет потоком данных в одно из своих свойств.
2. **Свойства (Properties):** Представляют состояние или данные, управляемые сервисом. Например, у `Fabricator` есть свойство `value`, которое хранит результат опроса. Когда это свойство изменяется, сервис может инициировать сигналы.
3. **Сигналы (Signals):** Позволяют сервису сообщать об изменениях состояния или событиях. Когда свойство `value` у `Fabricator` обновляется, излучается сигнал `changed`. Кроме того, излучается более специфичный сигнал, привязанный к самому свойству, — `notify::value`.

### II. Как определить сервис?

Определить сервис очень просто.

```python
from fabric.core.service import Service, Signal, Property

class NameService(Service):
    @Signal
    def name_changed(self, new_name: str) -> None: ...

    @Property(str, flags="read-write")
    def name(self) -> str:
        return self._name

    @name.setter
    def name(self, value: str):
        self._name = value
        self.name_changed(value) # Излучаем сигнал

    def __init__(self, name: str | None = None):
        super().__init__()
        self._name = name or ""

# Пример использования
name_service = NameService()
name_service.connect(
    "name-changed",
    lambda new_name: print(f"Имя изменилось, новое имя: {new_name}")
)
name_service.name = "Homan"
# Вывод: Имя изменилось, новое имя: Homan
```

### III. Сигналы (Signals)

Сигналы предоставляют простую модель управления состоянием, позволяя сервису уведомлять пользователей о возникновении определенных изменений. Сигнал также может передавать аргументы.

#### Определение сигналов

Чтобы определить сигнал, используйте декоратор `@Signal` внутри сервиса. Функция-обработчик сигнала должна быть полностью типизирована. Имя функции в `snake_case` (например, `signal_name`) автоматически преобразуется в `kebab-case` (`signal-name`).

```python
from fabric.core.service import Service, Signal

class NameService(Service):
    @Signal
    def name_changed(self, new_name: str) -> None: ...

    def __init__(self, **kwargs):
        super().__init__(**kwargs)
        self.name = ""

    def set_name(self, new_name: str) -> None:
        self.name = new_name
        # Emit the "name-changed" signal
        self.name_changed(new_name)
        # Alternative ways to emit a signal:
        # self.name_changed.emit(new_name)
        # self.emit("name-changed", new_name)
```

#### Подключения

Сервисы предлагают простой способ подключения сигналов к функциям обратного вызова непосредственно в конструкторе.

```python
def callback():
    print("Меня вызвали")

# Используем Fabricator в качестве примера сервиса
fabricator = Fabricator(
    poll_from=lambda: "hello there!",
    interval=1000,
    # Подключения сигналов
    on_changed=callback, # Подключает сигнал "changed"
    notify_value=lambda *_: print("свойство value изменилось") # Подключается к "notify::value"
)

# Альтернативные способы подключения
fabricator.connect("changed", callback)
fabricator.changed.connect(callback) # Работает специально для сервисов Fabric
```

### IV. Свойства (Properties)

Свойства в сервисе — это атрибуты, представляющие его состояние. Они основаны на `GObject.Property`, что обеспечивает нативную поддержку безопасности типов, уведомлений об изменении и возможность привязки к другим свойствам или элементам пользовательского интерфейса.

#### Определение свойств

Для определения свойства используется декоратор `@Property` из модуля `fabric.core.service`. Он требует указания типа данных и необязательных флагов.

```python
from fabric.core.service import Service, Property

class MyService(Service):
    @Property(str, flags="read-write")
    def status(self) -> str:
        return self._status

    @status.setter
    def status(self, value: str):
        self._status = value

    def __init__(self, status="", **kwargs):
        super().__init__(**kwargs)
        self._status = status
```

#### Уведомления о свойствах

`GObject.Property` автоматически излучает сигнал `notify::имя-свойства` при изменении значения. Есть несколько способов подключиться к этому сигналу:

1. **Через `connect`:**
   ```python
   example_service = MyService("Инициализация")
   # Подключаемся к сигналу notify::status для свойства status
   example_service.connect("notify::status", lambda *_: print("Статус изменился"))
   example_service.status = "Работает"
   ```
2. **Через аргумент в конструкторе:**
   Вы можете передать обратный вызов прямо в конструктор, используя имя `notify_<имя_свойства_в_snake_case>`.
   ```python
   example_service = MyService(
       initial_status="Инициализация",
       notify_status=lambda *_: print("Статус изменился")
   )
   example_service.status = "Работает"
   ```

#### Преимущества использования свойств и привязки (В РАЗРАБОТКЕ)

Разделы "Benefits of Using Properties" и "Property Bindings" в официальной документации находятся в разработке.

### V. Паттерн "Строитель" (Builder Pattern)

Паттерн "Строитель" позволяет конструировать сложные объекты шаг за шагом. В контексте сервиса вы можете выстраивать цепочки вызовов для установки свойств и подключения сигналов в одной строке, что упрощает процесс настройки.

```python
from fabric.core.fabricator import Fabricator

fabricator = Fabricator(
    poll_from=lambda: "hello there!",
    interval=1000,
).build() \
 .connect("changed", lambda *_: print("changed")) \
 .connect("notify::value", lambda *_: print("value notified")) \
 .set_value("initial value") \
 .unwrap() # Возвращает сам объект Fabricator, а не строитель
```

* `build()`: Инициализирует объект-строитель.
* `connect()`: Подключает сигналы (здесь: "changed" и "notify::value").
* `set_value()`: Устанавливает начальное значение.
* `unwrap()`: Возвращает сам экземпляр `Fabricator`, а не объект-строитель.

В качестве альтернативы можно настроить сервис с помощью функции обратного вызова, которая получает экземпляр сервиса (`self`) и объект-строитель (`builder`).

```python
fabricator = Fabricator(
    poll_from=lambda: "hello there!",
    interval=1000,
).build(lambda self, builder: builder
    .connect("changed", lambda *_: print("changed"))
    .connect("notify::value", lambda *_: print("value notified"))
    .set_value("initial value")
)
```

#### Преимущества использования "Строителя"

* **Читаемость:** Создает текучий интерфейс, делая код более читаемым.
* **Гибкость:** Можно настраивать сервисы в одну строку или использовать обратные вызовы для более сложных инициализаций.
* **Чистый код:** Уменьшает количество шаблонного кода.

---

## 10. Фабрикаторы (Guide: Fabricators)

**URL:** https://wiki.ffpy.org/guide/fabricators/

Фабрикаторы (`Fabricator`) — это мощный и один из наиболее часто используемых [сервисов](/guide/services/) в Fabric. Их основная задача — получать данные из внешних источников (команды оболочки) или внутренних источников (функции Python) и делать их доступными внутри приложения.

### I. Ключевые концепции

* **Опрос (Polling):** Фабрикатор периодически (с заданным `interval`) выполняет указанную команду или функцию и обновляет свое внутреннее значение.
* **Потоковая передача (Streaming):** Фабрикатор может непрерывно слушать вывод команды или функции-генератора, обновляя свое значение при поступлении новых данных.
* **Реактивность:** При каждом обновлении значения фабрикатор излучает сигнал `changed`, на который могут реагировать виджеты и другие компоненты.

### II. Параметры инициализации

`Fabricator` принимает несколько ключевых аргументов:

* `poll_from`: Источник данных. Может быть строкой (команда оболочки) или вызываемым объектом (функция/lambda).
* `interval` (int): Периодичность опроса в миллисекундах. Используется, только если `stream=False`.
* `stream` (bool): Если `True`, фабрикатор будет работать в режиме потоковой передачи. По умолчанию `False`.
* `default_value` (any): Начальное значение, которое будет у фабрикатора до первого успешного опроса.
* `on_changed` (callable): Функция обратного вызова, которая выполняется каждый раз, когда значение фабрикатора изменяется.

### III. Примеры использования

#### 1. Счетчик на Python

Простой пример, демонстрирующий опрос функции Python.

```python
from fabric.core.fabricator import Fabricator

# f - сам фабрикатор, v - его новое значение
counter_fabricator = Fabricator(
    interval=50,  # 50 мс
    default_value=0,
    poll_from=lambda f: f.get_value() + 1,  # Увеличиваем предыдущее значение на 1
    on_changed=lambda f, v: (
        (f.stop(), print("Counter Stopped")) if v == 43 else print(f"Counter Value: {v}")
    )
)
# Этот фабрикатор будет считать до 42, а затем остановится.
```

#### 2. Получение данных о погоде (команда оболочки)

Пример периодического выполнения shell-команды для получения данных.

```python
weather_fabricator = Fabricator(
    interval=1000 * 60,  # 1 минута
    poll_from="curl https://wttr.in/?format=Weather+in+%l:+%t+(Feels+Like+%f),+%C+%c",
    on_changed=lambda f, v: print(v.strip())
)
# Вывод: Weather in Homenland:, +15°C (Feels Like +15°C), Clear ☀️
```

#### 3. Получение текущей даты (команда оболочки)

```python
date_fabricator = Fabricator(
    interval=500,
    poll_from="date",
    on_changed=lambda f, v: print(f"Current Date: {v.strip()}"),
)
# Вывод: current date and time: Fri Nov 29 03:45:32 AM EET 2024
```

#### 4. Отслеживание статуса плеера (потоковая передача)

Этот пример показывает, как использовать режим `stream` для отслеживания непрерывного вывода.

```python
# ПРИМЕЧАНИЕ: для этой задачи лучше использовать специальный сервис Playerctl, если он доступен.
player_fabricator = Fabricator(
    stream=True,
    poll_from="playerctl --follow metadata --format '[{{status}}] {{title}} - {{artist}}'",
    on_changed=lambda f, v: print(v.strip())
)
# Вывод будет обновляться каждый раз, когда меняется статус плеера или трек.
# [Playing] Something - HomenArtsHouse
# [Paused] Something - HomenArtsHouse
```

#### 5. Отслеживание размера папки

Периодический опрос размера директории.

```python
# ПРИМЕЧАНИЕ: для этой задачи лучше использовать модуль `os` в Python.
documents_fabricator = Fabricator(
    interval=1000,  # 1 секунда
    poll_from="du -sh /home/homan/Documents/", # ПРИМЕЧАНИЕ: измените путь
    on_changed=lambda f, v: print(f"Size of Documents: {v.split()[0]}"),
)
# Вывод: Size of Documents: 1G
```

### IV. Запуск фабрикаторов

Важно помнить, что фабрикаторы (как и любые другие сервисы) не начнут работать, пока не будет запущен главный цикл приложения.

```python
from fabric.main import Application

# ... определения фабрикаторов

if __name__ == "__main__":
    app = Application()
    app.run()
```

После вызова `app.run()`, все созданные экземпляры `Fabricator` автоматически начнут свою работу (опрос или прослушивание потока).

---

## 11. Стилизация и темизация (Guide: Styling)

**URL:** https://wiki.ffpy.org/guide/styling/

Это руководство объясняет, как применять CSS для стилизации виджетов в Fabric.

### I. Основы: GTK и CSS

* Fabric использует GTK для рендеринга, поэтому для стилизации применяется CSS.
* **Рекомендуемые ресурсы для стилизации CSS в Fabric/GTK:**
  * [Учебник по CSS](https://www.w3schools.com/css/)
  * [Обзор CSS в GTK3](https://docs.gtk.org/gtk3/css-overview.html)
  * [Обзор свойств CSS в GTK3](https://docs.gtk.org/gtk3/css-properties.html)
* **Важно:** Хотя GTK поддерживает многие функции CSS, не все из веба работают в GTK. Всегда обращайтесь к документации GTK, чтобы проверить, что поддерживается.

### II. FASS: Встроенный компилятор CSS

Fabric поставляется с собственным препроцессором CSS под названием **FASS** (Fabric-CSS). Он компилирует CSS, написанный в привычном веб-стиле, в формат, понятный для GTK, например, превращая веб-CSS переменные в GTK-переменные `@define-*`.

FASS предлагает несколько полезных функций:

* Вызов функций Python из CSS
* Препроцессорные макросы
* Константы

FASS используется по умолчанию при загрузке стилей через функции `Application.set_stylesheet_*`. Чтобы отключить FASS, передайте `compile=False` функции, которая устанавливает ваш файл стилей.

**Подсказка:** FASS полностью совместим с нативными стилями CSS GTK.

#### 1. Обзор функций

Краткий список функций, поддерживаемых модулем FASS:

* **Веб-CSS переменные:** FASS преобразует стандартные веб-переменные (`--var-name`) в переменные GTK (`@define-color var-name`).

  * **Синтаксис:** Переменные должны быть объявлены внутри специального селектора `:vars`, чтобы они стали глобальными.

  ```css
  /* style.css (FASS) */
  :vars {
      --my-color: red;
  }
  #my-widget {
      background-color: var(--my-color);
  }
  ```

  * **Компилируется в:**

  ```css
  @define-color my-color red;
  #my-widget {
      background-color: @my-color;
  }
  ```
* **Константы:** Позволяют определять неизменяемые значения любого типа.

  ```css
  @define size-xl 12rem;
  @define wide-edges 2rem 4rem;

  #my-widget {
      padding: apply(size-xl);
  }
  #my-container {
      padding: apply(wide-edges);
  }
  ```

  * **Компилируется в:**

  ```css
  #my-widget {
      padding: 12rem;
  }
  #my-container {
      padding: 2rem 4rem;
  }
  ```
* **Макросы (Миксины):** Позволяют создавать переиспользуемые блоки стилей с параметрами.

  ```css
  @define my-macro(--color, --margin, --padding) {
      color: --color;
      margin: --margin;
      padding: --padding;
  }
  #my-widget {
      @apply my-macro(red, 0.5rem, 1rem);
  }
  ```

  * **Компилируется в:**

  ```css
  #my-widget {
      color: red;
      margin: 0.5rem;
      padding: 1rem;
  }
  ```
* **Вызов функций Python:** FASS может вызывать функции Python, переданные ему при загрузке таблицы стилей, и использовать их результаты как шаблон стиля.

  * **Python:**

  ```python
  app = Application(...)
  app.set_stylesheet_from_file(
      "./style.css",
      exposed_functions={
          "my-function": lambda prop, value: f"{prop}: {value};"
      },
  )
  ```

  * **CSS:**

  ```css
  #my-widget {
      @apply my-function(margin, 0.5rem);
      @apply my-function(padding, 1rem);
  }
  ```

  * **Компилируется в:**

  ```css
  #my-widget {
      margin: 0.5rem;
      padding: 1rem;
  }
  ```

#### 2. Зачем нужен FASS?

Основная идея встроенного компилятора CSS — устранить разрыв между способами определения таблиц стилей в GTK и веб-технологиях. Кроме того, FASS позволяет пользователям использовать обычные CSS-интеграции, такие как редакторы (LSP) и простые классовые CSS-библиотеки. Использование FASS не является обязательным, поскольку его введение лишь улучшает опыт разработки для пользователей Fabric. Вы можете использовать любой другой компилятор CSS вместо собственного Fabric; в конце концов, FASS — это всего лишь цепочка регулярных выражений, поэтому он далек от того, что есть на рынке.

### III. Лучшие практики

Лучшие практики стилизации в GTK не должны сильно отличаться от веб-стилей. Эти лучшие практики включают соглашения об именовании, использование имен (ID) и классов, а также согласованные единицы измерения. Следующий текст освещает все, что может понадобиться для начала стилизации с использованием лучших практик, однако следует знать, что это не полный список!

#### 1. Селекторы

| Веб CSS         | GTK CSS                      | Синтаксис                        | Описание и использование                                                                                                                                                                                                                                                                                                                                             |
| :----------------- | :--------------------------- | :---------------------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Тип (Type)      | Тип виджета        | `widget-type` (без префикса) | Стилизует все виджеты указанного типа (напр.,`button`, `label`). **Примечание:** Обычно не рекомендуется использовать селекторы типов, так как это может привести к слишком широким и трудноподдерживаемым стилям. |
| ID                 | Имя (`name`)            | `#widget-name` (префикс `#`)   | Стилизует конкретный виджет с заданным именем. Используйте для уникальных виджетов (напр.,`#main-button`, `#sidebar`). Избегайте использования имен для стилизации состояний или вариантов (напр., `#focused`).                          |
| Класс (Class) | Класс (`style-class`) | `.widget-class` (префикс `.`)  | Стилизует виджеты с определенным классом или набором классов. Лучше всего подходит для применения переиспользуемых стилей для конкретных состояний или вариантов (напр.,`.primary`, `.disabled`, `.highlight`).                      |

Для получения дополнительной информации о селекторах CSS мы рекомендуем прочитать этот замечательный блог.

#### 2. Соглашение об именовании

Мы настоятельно рекомендуем придерживаться `kebab-case` при написании всего, что связано с CSS, такого как имена файлов, селекторы и переменные.

Это также означает соблюдение следующих простых правил:

* **НЕ используйте** ничего, кроме `kebab-case`.
* **НЕ смешивайте** регистры.
* **НЕ используйте** ничего, кроме `-` в ваших именах для разделения слов.
* **НЕ ставьте** `-` после другого `-`.

**Примеры ПЛОХИХ стилей:**

```css
#My_Widget {
/* ... */
}

#My-Widget {
/* ... */
}

#myWidget {
/* ... */
}

#my-widget .Class--name {
/* ... */
}
```

**Примеры ХОРОШИХ стилей:**

```css
#my-widget {
/* ... */
}

#my-widget .class-name {
/* ... */
}
```

### IV. Применение стилей

#### 1. Стили по умолчанию и их сброс

Если вы не стилизуете свою конфигурацию, будет использоваться тема GTK по умолчанию в качестве таблицы стилей. Чтобы избавиться от таблицы стилей по умолчанию и начать стилизовать свои виджеты с нуля, вы можете начать с добавления файла `style.css` или, если вы собираетесь использовать несколько файлов CSS, добавить их в папку `styles/` и назвать свою точку входа `style.css`.

В файле `style.css` вам нужно будет добавить следующие несколько строк:

```css
*
{
    all: unset;
}
```

Это гарантирует, что любые другие уже установленные таблицы стилей не влияют на ваши виджеты.

Позже вы можете загрузить начальный CSS-файл, используя метод `Application.set_stylesheet_from_file("path/to/style.css")`.

#### 2. Встроенные (инлайн) стили

Дополнительно вы можете писать стили внутри определения вашего виджета.

**Примечание:** Встроенные определения стилей автоматически компилируются для вас. Чтобы отключить это, вам нужно будет использовать метод `set_style()` с параметром `compile=False`.

```python
Label(
    style="color: red; font-size: 1rem;"
)
# для примера мы используем виджет Label
# любой другой виджет Fabric должен работать так же
```

### V. Отладка и перезагрузка

#### 1. GTK Инспектор

Не уверены, почему что-то сломано (в плане стиля)? Окно инспектора GTK включает в себя массу инструментов для отладки стилей!

Чтобы показать инспектор, вы можете использовать именованный аргумент `inspect=True` в определении экземпляра вашего класса `Application`.

Альтернативно вы можете использовать переменную среды командной строки.

**Терминал:**

```bash
GTK_DEBUG=interactive python config.py
```

#### 2. Горячая перезагрузка стилей

Используйте этот сниппет, чтобы таблицы стилей перезагружались при изменении ваших CSS-файлов.

```python
app = Application(...)
from fabric.utils.helpers import monitor_file, get_relative_path

def apply_stylesheet(*_):
    return app.set_stylesheet_from_file(
        get_relative_path("./styles/style.css")
    )

style_monitor = monitor_file(get_relative_path("./styles"))
style_monitor.connect("changed", apply_stylesheet)
apply_stylesheet() # initial styling
# ...
```

---

## 12. Интеграция с оконными менеджерами (Guide: Window Managers)

**URL:** https://wiki.ffpy.org/guide/window-managers/

В этом разделе описываются встроенные в Fabric инструменты для глубокой интеграции с различными оконными менеджерами (WM), такими как Hyprland, i3 и Sway.

### I. Абстракции для WM

Fabric предоставляет набор абстрактных базовых виджетов, которые определяют общий API для взаимодействия с WM. Это позволяет писать более переносимый код. Для каждого поддерживаемого WM существуют конкретные реализации этих абстракций.

* **`WorkspaceButton`**: Специализированная кнопка для отображения одного рабочего стола. Ее состояние (`active`, `urgent`, `empty`) автоматически синхронизируется с состоянием реального рабочего стола и применяется в виде CSS-классов, что упрощает стилизацию.
  ```css
  /* Пример стилизации кнопок рабочих столов */
  #workspaces > button { background-color: #333; }
  #workspaces > button.active { background-color: #88b369; }
  #workspaces > button.urgent { background-color: #eb4d4b; }
  ```
* **`Workspaces`**: Контейнер, который автоматически управляет набором кнопок `WorkspaceButton`. Он слушает события от WM и добавляет, удаляет или обновляет кнопки по мере необходимости.
  * `buttons`: Список предопределенных `WorkspaceButton` для фиксированного количества рабочих столов.
  * `buttons_factory`: Функция для кастомизации создания каждой `WorkspaceButton` на основе ID рабочего стола.
* **`ActiveWindow`**: Виджет, отображающий информацию о текущем активном окне (например, его заголовок). Он слушает события изменения фокуса и динамически обновляет свое содержимое.
  * `formatter`: Объект `FormattedString`, который предоставляет `win_title` (заголовок окна) и `win_class` (первоначальный класс окна).
* **`Language`**: Виджет, показывающий текущую раскладку клавиатуры. Он слушает события изменения раскладки от оконного менеджера.
  * `keyboard`: Регулярное выражение для таргетирования конкретного устройства клавиатуры.
  * `formatter`: Объект `FormattedString`, который предоставляет `language` (текущий язык).

#### Утилита `FormattedString`

`FormattedString` — это ключевая утилита Fabric для динамического шаблонизирования в виджетах. Она использует заполнители (`{...}`) и предоставленные функции/переменные для создания шаблонов, которые реагируют на состояния виджетов. Переменные, специфичные для виджета (например, `win_title`, `win_class`, `language`), предоставляются автоматически.

* **Импорт:** `from fabric.utils import FormattedString`
* **Пример (обрезать длинный заголовок окна):**

  ```python
  from fabric.utils import FormattedString, truncate
  from fabric.hyprland.widgets import HyprlandActiveWindow

  # 'win_title' - переменная, которую виджет передает в FormattedString
  # 'truncate' - наша кастомная функция
  formatter = FormattedString(
      "{ 'Рабочий стол' if not win_title else truncate(win_title, 42) }",
      truncate=truncate,
  )

  active_window = HyprlandActiveWindow(formatter=formatter)
  ```
* **Пример (кастомное отображение языка):**

  ```python
  from fabric.hyprland.widgets import HyprlandLanguage
  from fabric.utils import FormattedString

  # 'language' - переменная от виджета
  lang_widget = HyprlandLanguage(
      formatter=FormattedString(
          "{ replace_lang(language) }",
          replace_lang=lambda lang: lang[:3].upper() # Отображать только первые 3 буквы в верхнем регистре
      )
  )
  ```

### II. Поддержка Hyprland

Fabric имеет нативную поддержку Hyprland через сокет IPC, что не требует никаких внешних зависимостей.

* **Сервис `Hyprland`**:
  * **Импорт:** `from fabric.hyprland import Hyprland` (или `from fabric.hyprland.service import Hyprland`)
  * **Отправка команд:** `Hyprland.send_command("dispatch workspace e+1")`
  * **Прослушивание событий:** `hyprland_service = Hyprland()` `hyprland_service.connect("event::workspacev2", callback)`
* **Виджеты для Hyprland:**
  * `HyprlandWorkspaces`: Этот виджет слушает события `createworkspacev2`, `destroyworkspacev2`, `workspacev2` и `focusedmonv2` для управления и отображения рабочих столов.
    * **Импорт:** `from fabric.hyprland.widgets import HyprlandWorkspaces`
  * `HyprlandActiveWindow`: Этот виджет слушает события `activewindow` и `closewindow` для отображения заголовка активного окна и автоматически обновляется при изменении фокуса.
    * **Импорт:** `from fabric.hyprland.widgets import HyprlandActiveWindow`
  * `HyprlandLanguage`: Этот виджет слушает событие `activelayout` для отображения текущей раскладки клавиатуры.
    * **Импорт:** `from fabric.hyprland.widgets import HyprlandLanguage`

### III. Поддержка i3 и Sway

Начиная с версии `v0.0.2`, добавлена аналогичная поддержка для i3 и Sway.

* **Сервис `I3`**:
  * **Импорт:** `from fabric.i3 import I3` (или `from fabric.i3.service import I3`)
  * **Отправка команд:**
    ```python
    from fabric.i3.service import I3, I3MessageType
    workspaces_reply = I3.send_command("", message_type=I3MessageType.GET_WORKSPACES)
    ```
  * **Прослушивание событий:**
    ```python
    from fabric.i3.service import I3, I3Event
    i3_connection = I3()
    i3_connection.connect("event::window::focus", lambda s, e: print(e.data))
    ```
* **Виджеты для i3/Sway:**
  * `I3Workspaces`: Виджет `Workspaces` для i3/Sway, который слушает события `workspace::focus`, `workspace::init` и `workspace::empty`.
    * **Импорт:** `from fabric.i3.widgets import I3Workspaces`
  * `I3ActiveWindow`: Виджет `ActiveWindow` для i3/Sway, который слушает события `window::focus`, `window::title` и `window::close` для индикации активных окон (имеет тот же API, что и `HyprlandActiveWindow`).
    * **Импорт:** `from fabric.i3.widgets import I3ActiveWindow`
  * `I3Language` (**только для Sway**): Виджет `Language`, который слушает событие `input::xkb_layout`. Этот виджет работает **только со Sway**, так как стандартный IPC i3 не предоставляет событий для изменения раскладки клавиатуры. Он имеет тот же API, что и `HyprlandLanguage`.
    * **Импорт:** `from fabric.i3.widgets import I3Language`

---

## 13. Галерея сниппетов (Snippets Gallery)

**URL:** https://wiki.ffpy.org/snippets/gallery/

Эта страница служит каталогом для различных фрагментов кода (сниппетов), предоставленных сообществом.

### 1. Изменение цвета заполнителя для Entry

* **Тип:** Сниппет стиля
* **Описание:** Предоставляет обходной путь для изменения цвета текста-заполнителя (placeholder) в виджете `Entry`, так как GTK не поддерживает это напрямую через CSS.
* **Автор:** EL-Darsh
* **Ссылка:** `https://wiki.ffpy.org/snippets/entry-placeholder-color/`

### 2. Аниматор свойств

* **Тип:** Сниппет сервиса
* **Описание:** Позволяет анимировать числовые свойства (int, float) в любом сервисе, который наследуется от `GObject.Object`.
* **Автор:** EL-Darsh
* **Ссылка:** `https://wiki.ffpy.org/snippets/property-animator/`

---

## 14. Сниппет: Изменение цвета заполнителя (Entry's Placeholder Color)

**URL:** https://wiki.ffpy.org/snippets/entry-placeholder-color/

Этот сниппет решает проблему изменения цвета текста-заполнителя (placeholder) в виджете `Entry`.

### I. Проблема

* Стандартные средства CSS в GTK не предоставляют прямого и очевидного способа стилизовать плейсхолдер виджета `Entry`. Автор столкнулся с этой проблемой и не нашел готовых решений в документации.

### II. Решение

После исследования исходного кода GTK было обнаружено, что цвет плейсхолдера можно изменить, определив специальную переменную в CSS.

**Ключевое открытие:** GTK ищет определение цвета с именем `placeholder_text_color`.

**Реализация:**

* Благодаря компилятору FASS, который преобразует веб-синтаксис CSS в понятный для GTK, решение становится очень простым.
* Нужно определить стандартную CSS-переменную `--placeholder_text_color` внутри селектора `:vars`.

**Код CSS:**

```css
:vars {
    --placeholder_text_color: red; /* Замените 'red' на любой желаемый цвет */
}
```

### III. Ограничения

* Найденный метод является **глобальным**. Это означает, что заданный цвет будет применен к плейсхолдерам **всех** виджетов `Entry` в приложении.
* На момент написания статьи способ применить этот стиль к какому-то **конкретному** виджету `Entry` (например, по его ID или классу) не был найден.

### IV. TL;DR (краткое изложение)

Чтобы изменить цвет текста-заполнителя для всех полей ввода, добавьте следующую строку в ваш CSS-файл в секцию `:vars`:

```css
--placeholder_text_color: #some-color;
```

---

## 15. Сниппет: Аниматор свойств (Property Animator)

**URL:** https://wiki.ffpy.org/snippets/property-animator/

Этот сниппет предоставляет сервис `Animator`, который позволяет плавно анимировать числовые свойства ( `float` или `int`) других сервисов или виджетов, используя кривые Безье для создания эффектов замедления/ускорения.

### I. Основная идея

* Многие виджеты (например, `CircularProgressBar`) не поддерживают CSS-анимацию для определенных свойств (таких как `value`).
* Этот сервис решает проблему, создавая цикл обновления, который плавно изменяет значение свойства от начального до конечного за заданный промежуток времени.

### II. Код сервиса `Animator`

Это сервис, который выполняет всю логику анимации.

```python
from typing import cast
from fabric import Service, Signal, Property
from gi.repository import GLib, Gtk

class Animator(Service):
    @Signal
    def finished(self) -> None: ...

    @Property(tuple)
    def bezier_curve(self):
        return self._bezier_curve

    @bezier_curve.setter
    def bezier_curve(self, value):
        self._bezier_curve = value

    @Property(float)
    def duration(self):
        return self._duration

    @duration.setter
    def duration(self, value):
        self._duration = value

    @Property(float)
    def value(self):
        return self._value

    @value.setter
    def value(self, value):
        self._value = value

    @Property(float)
    def min_value(self):
        return self._min_value

    @min_value.setter
    def min_value(self, value):
        self._min_value = value

    @Property(float)
    def max_value(self):
        return self._max_value

    @max_value.setter
    def max_value(self, value):
        self._max_value = value

    @Property(bool)
    def playing(self):
        return self._playing

    @playing.setter
    def playing(self, value):
        self._playing = value

    @Property(bool)
    def repeat(self):
        return self._repeat

    @repeat.setter
    def repeat(self, value):
        self._repeat = value

    def __init__(
        self,
        bezier_curve: tuple,
        duration: float,
        min_value: float = 0,
        max_value: float = 1,
        value: float | None = None,
        playing: bool = False,
        repeat: bool = False,
        tick_widget: Gtk.Widget | None = None,
        **kwargs,
    ):
        super().__init__(**kwargs)
        self._bezier_curve = bezier_curve
        self._duration = duration
        self._min_value = min_value
        self._max_value = max_value
        self._value = value if value is not None else min_value
        self._playing = playing
        self._repeat = repeat
        self._tick_widget = tick_widget
        self._tick_handler = None
        self._start_time = 0.0 # Changed to float for consistency
        self._timeline_pos = 0.0 # Added
        self.connect("notify::repeat", lambda *_: self.stop()) # If repeat changes while playing, stop it

    def do_get_time_now(self) -> float:
        return GLib.get_monotonic_time() / 1000000.0

    def do_lerp(self, start: float, end: float, time: float) -> float:
        return start + (end - start) * time

    def do_interpolate_cubic_bezier(self, time: float) -> float:
        # Kept from fabric_docs.md as it is correct and fetched was malformed
        return (
            (1.0 - time) ** 3 * self.bezier_curve[0]
            + 3.0 * (1.0 - time) ** 2 * time * self.bezier_curve[1]
            + 3.0 * (1.0 - time) * time**2 * self.bezier_curve[2]
            + time**3 * self.bezier_curve[3]
        )

    def do_ease(self, time: float) -> float:
        return self.do_lerp(
            self.min_value, self.max_value, self.do_interpolate_cubic_bezier(time)
        )

    def do_update_value(self, current_time: float):
        if not self.playing:
            return

        elapsed_time = current_time - self._start_time
        self._timeline_pos = min(1.0, elapsed_time / self.duration)

        self.value = self.do_ease(self._timeline_pos)

        if not self._timeline_pos >= 1.0:
            return

        if not self.repeat:
            self.value = self.max_value
            self.finished.emit()
            self.pause()
            return
    
        self.finished.emit()
        self._start_time = current_time
        self._timeline_pos = 0.0
    
    def do_handle_tick(self, *_):
        self.do_update_value(self.do_get_time_now())
        return True

    def do_remove_tick_handlers(self):
        if self._tick_handler:
            if self._tick_widget:
                self._tick_widget.remove_tick_callback(self._tick_handler)
            else:
                GLib.source_remove(self._tick_handler)
            self._tick_handler = None

    def play(self):
        if self.playing:
            return

        self._start_time = self.do_get_time_now()
        if not self._tick_handler:
            if self._tick_widget:
                self._tick_handler = self._tick_widget.add_tick_callback(self.do_handle_tick)
            else:
                GLib.timeout_add(16, self.do_handle_tick)
        self.playing = True
    
    def pause(self):
        if not self.playing:
            return

        self.playing = False
        self.do_remove_tick_handlers()

    def stop(self):
        self.pause()
        self.value = self.min_value
        self.playing = False
        self._start_time = 0.0
        self._timeline_pos = 0.0
        self.do_remove_tick_handlers()
```

* **Ключевые моменты:**
  * `GLib.timeout_add` или `Gtk.Widget.add_tick_callback` используются для регулярного вызова `do_handle_tick`.
  * `do_interpolate_cubic_bezier` вычисляет кривую для эффекта "easing".
  * Сигнал `finished` излучается по завершении анимации (если `repeat=False`).

### III. Пример использования

Этот пример показывает, как применить `Animator` к виджету `CircularProgressBar`.

```python
from fabric.widgets.circular_progress_bar import CircularProgressBar
# предполагается, что класс Animator определен в файле animator.py
from .animator import Animator

class AnimatedCircularProgressBar(CircularProgressBar):
    def __init__(self, **kwargs):
        super().__init__(**kwargs)
        # Создаем аниматор
        self.animator = Animator(
            bezier_curve=(0.34, 1.56, 0.64, 1.0), # кастомная кривая Безье
            duration=0.8,                         # длительность в секундах
            min_value=self.get_value(),           # начальное значение
            max_value=self.get_value(),           # конечное значение (пока равно начальному)
            tick_widget=self,                     # привязка к тикам виджета для синхронизации
        )
        # Подключаем сигнал изменения значения аниматора к обновлению прогресс-бара
        self.animator.connect("notify::value", lambda anim, *_: self.set_value(anim.value))
        # Подключаем сигнал завершения анимации (опционально)
        self.animator.connect("finished", lambda *_: print("Анимация завершена"))


    def set_value_animated(self, value: float):
        """Новый метод для анимированного изменения значения."""
        self.animator.pause()
        self.animator.min_value = self.get_value() # текущее значение прогресс-бара
        self.animator.max_value = value            # новое целевое значение
        self.animator.play()

# Как использовать:
# progress_bar = AnimatedCircularProgressBar()
# ...
# progress_bar.set_value_animated(75.0) # Анимированно установит значение на 75
```

* **Объяснение:**
  1. Создается новый класс `AnimatedCircularProgressBar`, который содержит экземпляр `Animator`.
  2. `Animator` привязывается к "тикам" самого виджета (`tick_widget=self`), что обеспечивает плавную анимацию, синхронизированную с кадрами GTK.
  3. Сигнал `notify::value` аниматора подключается к методу `set_value` прогресс-бара.
  4. Метод `set_value_animated` позволяет легко запускать анимацию до нового значения.

---

## 16. Руководство по вкладу в проект (Contributing: Hacking Guide)

**URL:** https://wiki.ffpy.org/contributing/hacking-guide/

Fabric создан как легко модифицируемый (hackable) пакет. Это руководство поможет вам вносить свои изменения в исходный код Fabric!

### I. Важные замечания

* **Версия GTK:** Fabric построен на **GTK 3**. Виджеты из других версий (например, GTK 4) несовместимы или требуют значительных усилий для работы.
* **Знание GTK/GObject:** Глубокое понимание этих технологий значительно улучшит ваши возможности по модификации и сократит количество проблем. Вот несколько полезных ресурсов для изучения GTK и GObject:
  * [Учебник Себастьяна по PyGObject](https://www.youtube.com/playlist?list=PL46788BA8A99C70C6)
  * [Официальный учебник PyGObject](https://pygobject.readthedocs.io/en/latest/guide/)
  * Документация, которую использует мейнтейнер
  * Книга "Foundations of PyGTK Development"
* **Портирование:** Если виджет, который вы пытаетесь реализовать, уже был реализован где-то еще (даже на другом языке) с использованием GTK (версии 3) и/или Cairo, портирование будет простым: вы можете просто использовать те же функции рисования, так как привязки Cairo не отличаются от языка к языку.

### II. Среда разработки

Прежде чем что-либо делать, вам следует подготовить среду разработки; для этого ознакомьтесь с [этой страницей](https://wiki.ffpy.org/contributing/development-environment/).

### III. Структура исходного кода

* **Виджеты:** В основе Fabric лежит очень особенный виджет: `Widget` (из `fabric.widgets.widget`). Почти каждый другой виджет Fabric наследует свойства и методы от этого виджета. Это упрощает внесение изменений, которые отражаются на всех виджетах.
* **Сервисы и объекты:** Fabric имеет базовый класс `Service` (в `fabric.service`). Новые сервисы Fabric должны наследоваться от этого класса. При этом убедитесь, что вы заменяете декоратор `@property` на `@Property` (импортированный из `fabric.service`), чтобы получать свойства с уведомлениями в вашем сервисе.

Для создания сигналов вы можете изменить переменную `__gsignals__`. `__gsignals__` принимает объект `SignalContainer` (из `fabric.service`), а этот контейнер сигналов принимает объекты `Signal` в качестве аргументов. Пример:

```python
from fabric.service import *

class MyUsefulService(Service):
    __gsignals__ = SignalContainer(
        Signal("my-really-useful-signal", "run-first", None, (object,))
        # имя сигнала, флаги выполнения, что возвращает callback, тип аргумента callback
    )

    def __init__(self):
        self.emit("my-really-useful-signal", "это мой суперполезный аргумент, переданный вам, какая полезная строка")
```

Более подробную информацию о сервисах можно найти на [этой вики-странице](/guide/services/).

### IV. Написание нового виджета

Чтобы начать писать свой собственный виджет, вам следует сначала проверить, включает ли GTK уже этот виджет; в конце концов, мы не хотим изобретать колесо. Если GTK реализует виджет, который вы ищете, то работа будет намного проще. Даже если нет, вам не о чем беспокоиться, так как эта страница также научит вас, как написать свой собственный виджет с нуля!

#### 1. Мой виджет существует в GTK

Сделайте одно из следующего, чтобы получить виджет:

* **Просто используйте его:** Fabric полностью совместим с виджетами GTK, поэтому вы можете импортировать виджет и использовать его по мере необходимости. Обратите внимание, что это не предоставит новому виджету все функции Fabric (т.е. установка стиля внутри конструктора), поэтому, если вам это требуется, см. следующий вариант.
* **"Фабрицируйте" виджет:** "фабрикация" виджета означает, что вы берете виджет GTK и преобразуете его в виджет Fabric; это позволит использовать функции Fabric, такие как специальные методы и свойства.

#### 2. Фабрикация виджета GTK

Чтобы начать "фабрикацию" виджета, сначала проверьте тип виджета, который вы хотите "фабрицировать". В общем, существует два типа виджетов: обычный виджет и контейнер.

* **Обычные виджеты** (также известные как не-контейнеры) — это виджеты, которые наследуют свойства и методы непосредственно от базового класса `Widget` и не могут содержать дочерние виджеты (например, метки и изображения).
* **Контейнеры** — это виджеты, которые наследуют свойства и методы от базового класса `Container` (который в конечном итоге наследует методы и свойства от базового виджета). Эти виджеты-контейнеры могут содержать один или несколько виджетов в качестве дочерних элементов (например, блоки и окна).

Далее вам следует определить, где находится ваш класс виджета. Например, если ваш виджет-не-контейнер называется `MyWidget` в GTK:

```python
import gi # импортируем репозиторий gi, чтобы получить GTK позже

gi.require_version("Gtk", "3.0") # получаем версию 3 GTK, так как Fabric использует именно ее

from gi.repository import Gtk # теперь GTK (версия 3) импортирован

from fabric.widgets.widget import Widget # импортируем базовый виджет Fabric

class MyFabricatedWidget(Gtk.MyWidget, Widget): # создаем новый класс с именем "MyFabricatedWidget", который наследует желаемый виджет GTK и базовый виджет Fabric

    def __init__(self, **kwargs): # функция инициализатора, **kwargs (словарь) означает, что любые дополнительные аргументы будут в этом словаре
        # вы можете добавить больше логики сюда, чтобы обрабатывать новые аргументы (если есть)
        super().__init__(**kwargs) # инициализирует новый смешанный класс
```

"Фабрикация" виджета-контейнера — это тот же процесс, что и "фабрикация" обычного виджета; просто используйте виджет `Container` из Fabric вместо `Widget` при наследовании.

### V. Отладка

О, вы столкнулись с проблемой. Не паникуйте! Если ваш код вообще не работает, прочитайте ошибку; чаще всего она предоставляет много информации (например, трассировку стека вызовов).

Существуют некоторые специфические ошибки, которые может быть сложнее отлаживать, например, ошибки, производимые самим GTK из-за вызова вашей функции/метода. Эти ошибки, в большинстве случаев, предоставляют только ошибку утверждения (AssertionError) (которая может быть полезна для получения трассировки стека). Вот как отлаживать эти невидимые "Heisenbug":

* **Проверьте переменные окружения:**
  Например, вы пытаетесь запустить окно X11 в X11, но переменная окружения `GDK_BACKEND` установлена на `wayland` из-за того, что вы принудительно установили ее таким образом и забыли. Это была проблема, из-за которой ваше окно Wayland открывалось как обычное окно, а не как слой. (Другая возможность здесь заключалась в том, что вы использовали GNOME, который не реализовал протокол `layer-shell` в Wayland и поэтому делает невозможным что-либо для исправления проблем с ним.)
* **Используйте отладчик и точки останова**
  Например, вы решаете использовать функцию, которая, как вы знаете, существует в исходном коде пакета репозитория GI, который вы пытаетесь использовать, но ваш код вызывает ошибку сегментации каждый раз, когда вы его запускаете. (Позже, читая документацию, вы выяснили, что эта конкретная функция не была доступна для используемой вами привязки.)
* **Проверьте потокобезопасность**
  Например, вы создаете новый сервис, и он прекрасно работает. Однако через неизвестное время все рушится. Позже вы узнали от друга, что обновляли свойства объекта, определенного в основном потоке, в другом потоке, что привело к потере потокобезопасности. После обнаружения этой проблемы вы нашли функцию, которая делала то же самое, что вам нужно было сделать (например, `GLib.idle_add`), потокобезопасным способом.
* **Переопределение переменных или методов**
  Например, вы создаете новый класс, который наследует `Service`. Затем вы пишете новый метод: `connect`, но когда вы запускаете код, вы получаете `ArgumentError`, хотя передаете все необходимое в метод. Затем вы обнаруживаете, что класс `Service` уже имеет метод `connect`, который используется внутри, поэтому, когда вы пытаетесь использовать `connect` в своем новом классе, вы фактически вызываете метод в классе `Service`. Вы исправили эту проблему, используя другой класс для наследования, хотя вы также можете решить эту проблему, просто переименовав метод.

### VI. Визуальные ошибки

Существует еще один вид ошибок — визуальные ошибки. Визуальная ошибка означает, что код работает так, как ожидалось, но есть графические проблемы с рендерингом или отрисовкой. Отличным инструментом для решения этих проблем с Fabric или GTK является GTK Inspector.

Чтобы открыть окно инспектора в Fabric, установите переменную окружения `GTK_DEBUG` на `interactive` перед запуском кода.

Если вы все еще не можете понять, что заставляет ваш код работать не так, как должен, вы всегда можете зайти на Discord-сервер Fabric и задать свой вопрос — мы будем рады помочь!

---

## 17. Среда разработки (Contributing: Development Environment)

**URL:** https://wiki.ffpy.org/contributing/development-environment/

Это руководство описывает шаги для настройки локальной среды разработки для внесения вклада в проект Fabric.

### I. Получение исходного кода

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/Fabric-Development/fabric
   ```
2. **Перейдите в директорию проекта:**
   ```bash
   cd fabric
   ```

### II. Настройка виртуального окружения

Использование виртуального окружения Python (`venv`) является обязательным для изоляции зависимостей.

1. **Создайте виртуальное окружение:**

   ```bash
   python -m venv venv
   ```
2. **Активируйте его:**

   ```bash
   source venv/bin/activate
   ```

   *Ваша командная строка должна измениться, чтобы показать, что вы находитесь внутри окружения `(venv)`.*

### III. Установка зависимостей и Fabric в режиме редактирования

1. **Установите зависимости:**

   ```bash
   pip install -r requirements.txt
   ```
2. **Установите Fabric в "режиме редактирования":**

   ```bash
   pip install -e .
   ```

   * **Флаг `-e`** (или `--editable`) очень важен. Он создает символическую ссылку на исходный код, а не копирует его. Это означает, что любые изменения, которые вы вносите в код, немедленно отражаются в установленном пакете без необходимости переустановки.

После выполнения этих шагов ваша среда разработки готова, и вы можете приступать к написанию кода, следуя [руководству по хакингу](/contributing/hacking-guide/).

---

## 18. Руководство по стилю кода (Contributing: Code Style Guide)

**URL:** https://wiki.ffpy.org/contributing/code-style-guide/

Это руководство определяет стандарты кодирования для проекта Fabric, чтобы обеспечить единообразие и читаемость кодовой базы.

### I. Форматирование

* **Инструмент:** Весь код должен быть отформатирован с помощью **`ruff`**.
* **Отступы:** Строго **4 пробела**. Табуляция запрещена.

### II. Соглашения об именовании

| Контекст                          | Стиль               | Пример                    |
| ----------------------------------------- | ------------------------ | ------------------------------- |
| Функции, Переменные      | `snake_case` (з "_")  | `my_variable`, `do_stuff()` |
| Константы                        | `SCREAMING_SNAKE_CASE` | `SOME_CONSTANT`               |
| Классы                              | `PascalCase`           | `MyAwesomeWidget`             |
| Сигналы/Свойства (GObject) | `kebab-case` (с "-")  | `my-signal`, `my-property`  |

### III. Комментарии

* Комментарии должны следовать стилю, описанному в **[PEP 8](https://peps.python.org/pep-0008/)**.
* **Правильно:**
  ```python
  # Это хороший комментарий.
  ```
* **Неправильно:**
  ```python
  ##Так не надо##
  #--И так тоже--#
  ```

### IV. Логирование

* **Библиотека:** Для логирования используется **`loguru`**.
* **Правила:**
  * Будьте краткими.
  * Не оставляйте в коде вызовы `logger.debug()`.
  * `logger.error()` следует использовать только для критических ошибок, нарушающих работу всей программы.
  * `logger.info()` следует использовать умеренно в низкоуровневых классах/модулях, за исключением подклассов сервисов.
  * Если сомневаетесь — спросите в pull request.

### V. Условные операторы

* **Тернарный оператор (в одну строку):** Используйте, если нужно изменить значение **только одной** переменной.

  ```python
  # Хорошо
  message = "Успех" if status == 200 else "Ошибка"
  ```
* **Стандартный `if/else`:** Используйте для выполнения блока кода из нескольких строк.

  ```python
  # Хорошо
  if status == 200:
      message = "Успех"
      is_valid = True
      do_something()
  ```

  Это более читаемо, чем несколько тернарных операторов подряд.

### VI. Сообщения коммитов

* Проект следует спецификации **[Conventional Commits](https://www.conventionalcommits.org/en/v1.0.0/)**.
* **Формат:** `<тип>[опциональная область]: <описание>`
* **Основные типы:**
  * `feat`: Для новой функциональности.
  * `fix`: Для исправления ошибок.
* **Примеры:**
  * `feat: Добавить поддержку виджета погоды`
  * `fix(services): Исправить утечку памяти в сервисе аудио`
  * `feat(css)!: Изменить API переменных FASS` (восклицательный знак означает **BREAKING CHANGE**).

### VII. Журнал изменений (CHANGELOG.md)

* Перед созданием pull request необходимо обновить файл `CHANGELOG.md`.
* Запись должна быть краткой и понятной для пользователя.
* **Формат:** `<Порядковый номер>. <#Номер PR> <Тип коммита>: <Сообщение коммита>`
  ```
  1. #123 feat: Добавлена новая фича!
  2. #124 fix: Исправлена ошибка, которая приводила к крешу (closes #122)
  ```

---

## 19. Справочник API (API Reference)

**URL:** https://wiki.ffpy.org/api/fabric/

Эта страница является центральным узлом для всей документации по API Fabric. Она содержит ссылки на подробные описания всех доступных модулей, включая:

### I. Сервисы (Services)

* `Audio` / `AudioStream`: Управление аудиопотоками и устройствами.
* `Bluetooth` / `BluetoothDevice`: Взаимодействие с Bluetooth-устройствами.
* `Hyprland`: Интеграция с оконным менеджером Hyprland.
* `i3`: Интеграция с i3 и Sway.
* `Notifications`: Управление системными уведомлениями.
* `SystemTray`: Работа с элементами системного трея.
* **Дополнительные классы сервисов/исключения/перечисления:**
  * `CvcImportError`
  * `NotificationCloseReason`
  * `NotificationImagePixmap`
  * `NotificationAction`
  * `NotificationSerializedData`
  * `HyprlandError`, `HyprlandSocketError`, `HyprlandSocketNotFoundError`, `HyprlandEvent`, `HyprlandReply`
  * `SystemTrayItemPixmap`, `SystemTrayItemToolTip`

### II. Утилиты (Utilities)

* `PixbufUtils`: Набор статических методов для работы с `GdkPixbuf.Pixbuf`.
* `FormattedString`: Мощный форматтер строк для динамического обновления текста.
* `DesktopApp`: Класс-контейнер для информации о `.desktop` файле приложения.
* **Вспомогательные функции:**
  * `get_desktop_applications()`, `parse_color()`, `get_gdk_rgba()`, `compile_css()`, `bulk_replace()`, `bulk_connect()`, `bulk_disconnect()`, `clamp()`, `extract_css_values()`, `monitor_file()`, `cooldown()`, `exec_shell_command()`, `exec_shell_command_async()`, `invoke_repeater()`, `get_relative_path()`, `load_dbus_xml()`, `snake_case_to_pascal_case()`, `pascal_case_to_snake_case()`, `snake_case_to_kebab_case()`, `kebab_case_to_snake_case()`, `get_enum_member()`, `get_enum_member_name()`, `bridge_signal()`, `generate_random_string()`, `FunctionAnnotations`, `get_function_annotations()`, `truncate()`, `idle_add()`, `remove_handler()`, `get_connectables_for_kwargs()`, `set_stylesheet_from_file()`, `set_stylesheet_from_string()`, `idlify()`

### III. Виджеты (Widgets)

* **Базовые:** `Widget`, `Container`, `Label`, `Button`, `Image`, `Box`, `Fixed`.
* **Контейнеры-раскладки:** `CenterBox`, `FlowBox`, `Overlay`, `Stack`, `Revealer`.
* **Специализированные:** `CircularProgressBar`, `DateTime`, `Entry`, `EventBox`, `Scale`, `ScrolledWindow`, `WebView`, `Svg`.
* **Специфичные для WM:** `WaylandWindow`, `X11Window`, `HyprlandWorkspaces`, `I3Workspaces`, `HyprlandActiveWindow`, `I3ActiveWindow`, `HyprlandLanguage`, `I3Language`.
* **Дополнительные перечисления/классы:**
  * `WaylandWindowExclusivity`
  * `X11WindowLayer`
  * `X11WindowGeometry`

### IV. Фигуры (Shapes)

* **`Corner`**, **`Star`**: Пользовательские фигуры, которые можно использовать в виджетах.

Эта страница API является отправной точкой для изучения конкретных классов, их свойств, методов и сигналов.

---

## 20. API сервиса: Аудио (API: Audio Service)

**URL:** https://wiki.ffpy.org/api/services/audio/

Этот модуль предоставляет классы для управления аудио на системном уровне.

### I. `CvcImportError`

Базовый класс: `ImportError`
Вызывается, если не удается импортировать необходимую библиотеку `libcvc` (решается установкой `cinnamon-desktop`).

### II. `AudioStream`

Представляет один аудиопоток, будь то приложение, динамик или микрофон. Этот класс также позволяет управлять содержащимся потоком (например, громкостью и состоянием отключения звука).

* **Наследуется от:** `Service`
* **Ключевые свойства:**
  * `icon_name` (str): Рекомендуемое имя иконки для этого типа потока.
  * `id` (int): Идентификатор этого потока.
  * `name` (str): Отображаемое имя, связанное с этим потоком.
  * `description` (str): Текст, описывающий назначение этого потока.
  * `application_id` (str): Идентификатор приложения этого потока (если есть).
  * `state` (str): Текущее состояние этого потока.
  * `stream`: Фактический объект потока, который удерживает этот класс.
  * `volume` (float): Текущее значение громкости для этого потока (ограничено `max_volume` родителя).
  * `muted` (bool): Находится ли этот поток в состоянии отключения звука.
  * `type` (str): Тип потока (`"speaker"`, `"microphone"`, `"application"`, `"recorder"`).
* **Ключевые сигналы:**
  * `changed`: Излучается при любом изменении состояния потока.
  * `closed`: Излучается при закрытии потока.
* **Ключевые методы:**
  * `notifier()`
  * `close()`

### III. `Audio`

Основной сервис для взаимодействия с аудиосистемой.

* **Наследуется от:** `Service`
* **Параметры:**
  * `max_volume` (int, optional): Максимальная громкость по умолчанию для всех потоков; это ограничение работает только при установке громкости через этот сервис, по умолчанию `100`.
  * `controller_name` (str, optional): Имя микшера этого сервиса, по умолчанию "fabric audio control".
* **Ключевые свойства:**
  * `speaker` (AudioStream): Активный потоковый динамик по умолчанию (если есть).
  * `speakers` (list[AudioStream]): Список всех потоковых динамиков, подключенных к этому устройству.
  * `microphone` (AudioStream): Активный потоковый микрофон по умолчанию (если есть).
  * `microphones` (list[AudioStream]): Список всех потоковых микрофонов, подключенных к этому устройству.
  * `applications` (list[AudioStream]): Список всех потоков приложений, выводящих звук в данный момент.
  * `recorders` (list[AudioStream]): Список потоков приложений, записывающих звук в данный момент.
  * `max_volume` (int): Максимальное значение громкости, которого могут достичь все потоки при установке громкости через этот сервис.
  * `state`
* **Ключевые сигналы:**
  * `changed`
  * `speaker-changed`: Излучается при смене динамика по умолчанию.
  * `microphone-changed`: Излучается при смене микрофона по умолчанию.
  * `stream-added`: Излучается при появлении нового аудиопотока.
  * `stream-removed`: Излучается при исчезновении аудиопотока.
* **Ключевые методы:**
  * `get_stream_type()`

---

## 21. API сервиса: Bluetooth (API: Bluetooth Service)

**URL:** https://wiki.ffpy.org/api/services/bluetooth/

Этот модуль предоставляет классы для управления Bluetooth-устройствами через D-Bus.

### I. `BluetoothDevice`

Представляет одно Bluetooth-устройство.

* **Наследуется от:** `Service`
* **Ключевые свойства:**
  * `name` / `alias` (str): Имя устройства.
  * `address` (str): MAC-адрес устройства.
  * `connected` (bool): `True`, если устройство подключено.
  * `connecting` (bool): `True`, если идет процесс подключения.
  * `paired` (bool): `True`, если устройство сопряжено.
  * `trusted` (bool): `True`, если устройство является доверенным.
  * `icon_name` (str): Имя иконки, соответствующее типу устройства.
  * `type` (str): Тип устройства (например, "headset", "mouse").
  * `battery_level` / `battery_percentage` (int/float): Уровень заряда батареи (если поддерживается).
  * `device`: Объект устройства.
* **Ключевые методы:**
  * `connect_device(connect: bool = True, ...)`: Подключает или отключает устройство.
  * `close()`: Закрывает соединение с устройством.
  * `notifier()`: Метод для получения уведомлений.
* **Ключевые сигналы:**
  * `changed`: Излучается при любом изменении состояния устройства.
  * `closed`: Излучается при закрытии соединения.

### II. `BluetoothClient`

Основной сервис для управления Bluetooth-адаптером и обнаруженными устройствами.

* **Наследуется от:** `Service`
* **Ключевые свойства:**
  * `devices` (list[BluetoothDevice]): Список всех обнаруженных устройств.
  * `connected_devices` (list[BluetoothDevice]): Список только подключенных устройств.
  * `scanning` (bool): `True`, если идет процесс сканирования.
  * `enabled` / `powered` (bool): `True`, если Bluetooth-адаптер включен.
  * `address` (str): MAC-адрес локального адаптера.
  * `state`: Текущее состояние клиента.
* **Ключевые методы:**
  * `toggle_power()`: Включает/выключает адаптер.
  * `toggle_scan()`: Запускает/останавливает сканирование устройств.
  * `get_device(address: str)`: Возвращает объект `BluetoothDevice` по его адресу.
  * `connect_device(device, ...)`: Подключает/отключает указанное устройство.
  * `scan()`: Метод для запуска/остановки сканирования.
  * `notifier()`: Метод для получения уведомлений.
* **Ключевые сигналы:**
  * `device-added`: Излучается при обнаружении нового устройства.
  * `device-removed`: Излучается, когда устройство больше не обнаруживается.
  * `changed`: Излучается при любом изменении состояния клиента.
  * `closed`: Излучается при закрытии соединения.

---

## 22. API сервиса: Hyprland (API: Hyprland Service)

**URL:** https://wiki.ffpy.org/api/services/hyprland/

Этот модуль обеспечивает нативную интеграцию с IPC (межпроцессное взаимодействие) композитора Hyprland. Он позволяет отправлять команды и прослушивать события без внешних зависимостей.

### I. Обзор классов и исключений

* **`HyprlandError`**: Базовое исключение для ошибок, связанных с Hyprland.
* **`HyprlandSocketError`**: Возникает при проблемах с сокетом.
* **`HyprlandSocketNotFoundError`**: Возникает, если сокет Hyprland не найден (возможно, Hyprland не запущен).
* **`HyprlandEvent`**: Инкапсулирует данные о событии, полученном от Hyprland.
  * `name` (str): Имя события (например, `workspace`).
  * `data` (list[str]): Данные, связанные с событием.
  * `raw_data` (bytes): Данные в исходном виде, как они получены от сокета события.
* **`HyprlandReply`**: Инкапсулирует ответ на команду, отправленную в Hyprland.
  * `command` (str): Отправленная команда.
  * `reply` (bytes): "Сырой" ответ от сокета.
  * `is_ok` (bool): `True`, если Hyprland вернул `ok`.

### II. Класс `Hyprland`

Основной класс-сервис для взаимодействия с Hyprland.

* **Наследуется от:** `Service`
* **Конструктор:** `Hyprland(commands_only: bool = False, **kwargs)`
  * Если `commands_only=True`, сервис будет подключаться только к сокету для команд, но не для событий.
* **Члены класса:**
  * `EVENTS_SOCKET`: Сокет для событий.
  * `COMMANDS_SOCKET`: Сокет для команд.
  * `SOCKET_PATH`: Путь к сокету.
* **Ключевые сигналы:**
  * `ready`: Излучается, когда сервис готов к работе.
  * `event`: Излучается при получении любого события от Hyprland. Полезен для отладки.
  * `event::имя-события`: Для каждого события Hyprland (например, `workspace`, `focusedmon`, `destroywindow`) создается отдельный сигнал. Это основной способ реагировать на конкретные события.
* **Совместимость:** Сервис автоматически определяет правильный путь к сокету IPC, поддерживая как старые, так и новые версии Hyprland (начиная с v0.40.0).

### III. Статические методы

Эти методы можно вызывать напрямую из класса `Hyprland` без создания его экземпляра.

* **`lookup_socket() -> tuple[UnixSocketAddress, UnixSocketAddress, str]`**
  * Выполняет поиск сокета Hyprland.
* **`send_command(command: str) -> HyprlandReply`**
  * Синхронно отправляет команду в Hyprland (аналогично `hyprctl`).
  * **Пример:** `Hyprland.send_command("/dispatch workspace e+1")`
  * **Параметры:** `command` (str) — команда Hyprland для отправки.
  * **Возвращает:** Объект ответа команды, содержащий данные от Hyprland.
  * **Тип возвращаемого значения:** `HyprlandReply`
* **`send_command_async(command: str, callback: Callable, ...)`**
  * Асинхронно отправляет команду. Результат будет передан в указанную `callback`-функцию.
  * **Параметры:**
    * `command` (str) — команда Hyprland для отправки.
    * `callback` (Callable) — функция обратного вызова, в которую будет передан ответ команды.

### IV. Пример использования

```python
from fabric.hyprland.service import Hyprland

# Создаем экземпляр сервиса для прослушивания событий
hyprland_service = Hyprland()

# Определяем функцию-обработчик для события смены рабочего стола
def on_workspace_change(service, event_data):
    # event_data - это экземпляр HyprlandEvent
    print(f"Активный рабочий стол изменился: {event_data.data}")

# Подключаемся к конкретному сигналу
hyprland_service.connect("event::workspacev2", on_workspace_change)

# Также можно отправлять команды
Hyprland.send_command("/dispatch exec kitty")
```

---

## 23. API сервиса: Уведомления (API: Notifications Service)

**URL:** https://wiki.ffpy.org/api/services/notifications/

Этот модуль предоставляет полноценный сервер уведомлений, соответствующий спецификации `freedesktop.org`. Для его корректной работы другие демоны уведомлений (например, `dunst`, `mako`) должны быть отключены.

### I. `NotificationCloseReason` (Enum)

Перечисление, определяющее причину закрытия уведомления:

* `EXPIRED` (истекло по таймауту)
* `DISMISSED_BY_USER` (отклонено пользователем)
* `CLOSED_BY_APPLICATION` (закрыто приложением-отправителем)
* `UNKNOWN` (неизвестная причина)

### II. `NotificationImagePixmap`

Класс для хранения данных изображения, связанных с уведомлением.

* **Наследуется от:** `object`
* **Ключевые методы:**
  * `deserialize(data: tuple) -> NotificationImagePixmap`: Загружает данные изображения из сериализованного кортежа.
  * `as_pixbuf() -> GdkPixbuf.Pixbuf`: Загружает версию Pixbuf этого pixmap.
  * `serialize() -> tuple`: Сериализует изображение pixmap в кортеж для удобства передачи и сохранения.

### III. `NotificationAction`

Действие уведомления, которое может быть вызвано.

* **Наследуется от:** `object`
* **Ключевые свойства:**
  * `identifier`
  * `label`
  * `parent`
* **Ключевые методы:**
  * `invoke()`: Вызывает это действие.

### IV. `NotificationSerializedData`

Представляет собой `TypedDict` для сериализованных данных уведомления.

* **Ключевые свойства:**
  * `id`
  * `summary`
  * `body`
  * `timeout`
  * `urgency`
  * `actions`

### V. `Notification`

Класс уведомления содержит все данные конкретного уведомления (такие как действия, изображения и информация об отправителе).

* **Наследуется от:** `Service`
* **Ключевые сигналы:**
  * `closed`: Излучается при закрытии уведомления.
  * `action-invoked`: Излучается при выполнении действия.
* **Ключевые свойства:**
  * `app_name` (str): Отображаемое имя приложения, отправившего это уведомление.
  * `app_icon` (str): Необязательное имя иконки приложения.
  * `summary` (str): Заголовок уведомления.
  * `body` (str): Многострочный текст, предоставленный отправителем (может содержать разметку).
  * `id` (int): Уникальный идентификатор этого уведомления.
  * `replaces_id` (int): Необязательный ID существующего уведомления, которое это уведомление должно заменить.
  * `timeout` (int): Тайм-аут истечения срока действия (в миллисекундах), после которого уведомление должно автоматически закрыться.
  * `urgency` (int): Уровень срочности этого уведомления (0 - низкий, 1 - нормальный, 2 - критический).
  * `actions` (list[NotificationAction]): Список всех действий, которые есть у этого уведомления.
  * `image_pixmap` (NotificationImagePixmap): Необработанные данные изображения, предоставленные отправителем (если есть).
  * `image_file` (str): Путь к файлу изображения, предоставленный отправителем для этого уведомления (если есть).
  * `image_pixbuf` (GdkPixbuf.Pixbuf): Pixbuf, загруженный из свойства `image-pixmap` или `image-file`.
* **Ключевые методы:**
  * `deserialize(data: NotificationSerializedData) -> Notification`: Десериализует предоставленные сериализованные данные уведомления в только что созданный объект уведомления.
  * `serialize() -> NotificationSerializedData`: Сериализует это уведомление в словарь, который может быть легко преобразован в JSON.
  * `invoke_action(action_name: str)`: Вызывает действие по его имени.
  * `close(reason: Literal["expired", "dismissed-by-user", "closed-by-application", "unknown"] | NotificationCloseReason = NotificationCloseReason.DISMISSED_BY_USER)`: Закрывает это уведомление и уведомляет отправителя о причине.

### VI. `Notifications` (Сервер)

Основной класс, который действует как сервер уведомлений.

* **Наследуется от:** `Service`
* **Ключевые свойства:**
  * `notifications` (dict[int, Notification]): Словарь, где ключи — это ID уведомлений, а значения — объекты `Notification`.
* **Ключевые сигналы:**
  * `changed`: Излучается при любом изменении состояния сервера.
  * `notification-added`: Излучается при получении нового уведомления. Передает ID уведомления.
  * `notification-removed`: Излучается после удаления уведомления из списка.
  * `notification-closed`: Излучается при закрытии уведомления. Передает ID и причину закрытия.
* **Ключевые методы:**
  * `new_notification_id() -> int`: Получает следующий ID уведомления и увеличивает внутренний счетчик.
  * `get_notification_from_id(notification_id: int) -> Notification | None`: Ищет уведомление по его идентификатору.
  * `invoke_notification_action(notification_id: int, action: str)`: Вызывает именованное действие для уведомления.
  * `remove_notification(notification_id: int)`: Удаляет уведомление (без закрытия) с сервера.
  * `close_notification(notification_id: int, reason: NotificationCloseReason = NotificationCloseReason.DISMISSED_BY_USER)`: Закрывает уведомление и удаляет его с сервера.
    * **Примечание:** Рекомендуется использовать `Notification.close` вместо этого; этот метод предназначен для внутреннего использования и может быть изменен.
  * `serialize() -> list[NotificationSerializedData]`: Сериализует все уведомления на сервере.
  * `deserialize(data: list[NotificationSerializedData])`: Загружает список сериализованных данных уведомлений.

---

## 24. API сервиса: Системный трей (API: System Tray Service)

**URL:** https://wiki.ffpy.org/api/services/system_tray/

Этот модуль реализует спецификацию `StatusNotifierItem` от `freedesktop.org` для создания и управления элементами в системном трее (области уведомлений).

### I. `SystemTrayItemPixmap`

Класс для хранения данных изображения, связанных с элементом трея.

* **Наследуется от:** `NamedTuple`
* **Ключевые свойства:**
  * `width` (int): Ширина иконки в пикселях.
  * `height` (int): Высота иконки в пикселях.
  * `data`: Фактические данные, представляющие эту иконку.
* **Ключевые методы:**
  * `as_pixbuf() -> GdkPixbuf.Pixbuf | None`: Загружает Pixbuf-вариант этого pixmap.

### II. `SystemTrayItemToolTip`

Класс для хранения данных всплывающей подсказки, связанных с элементом трея.

* **Наследуется от:** `NamedTuple`
* **Ключевые свойства:**
  * `icon_name` (str): Имя иконки, соответствующее FreeDesktop.
  * `icon_pixmap` (SystemTrayItemPixmap): Данные иконки в виде pixmap.
  * `title` (str): Заголовок для этой всплывающей подсказки.
  * `description` (str): Описательный текст для этой всплывающей подсказки. Может содержать подмножество разметки HTML.

### III. `SystemTrayItem`

Представляет один элемент (иконку) в системном трее.

* **Наследуется от:** `Service`
* **Ключевые свойства:**
  * `id` (int): Идентификатор, присвоенный приложением.
  * `identifier` (str): Локальный идентификатор этого элемента трея, основанный на D-Bus приложения-владельца.
  * `title` (str): Заголовок приложения, владеющего этим элементом трея.
  * `status` (str): Строка, представляющая статус приложения этого элемента. `"passive"` означает "бездействует" и не содержит важных сообщений, `"active"` означает, что этот элемент должен быть отображен пользователю (например, срочный статус), и `"unknown"` означает невозможность получить статус.
  * `category` (str): Категория этого элемента.
  * `window_id`: ID окна.
  * `icon_theme_path`: Путь к теме иконок.
  * `icon_theme`: Тема иконок.
  * `icon_name` (str): Имя иконки элемента.
  * `icon_pixmap` (SystemTrayItemPixmap): Иконка элемента в виде pixmap.
  * `overlay_icon_name`: Имя накладываемой иконки.
  * `overlay_icon_pixmap`: Накладываемая иконка в виде pixmap.
  * `attention_icon_name`: Имя иконки внимания.
  * `attention_icon_pixmap`: Иконка внимания в виде pixmap.
  * `tooltip` (SystemTrayItemToolTip): Объект, содержащий данные для всплывающей подсказки.
  * `is_menu`: Является ли элемент меню.
  * `menu_object_path`: Путь объекта меню.
  * `menu` (Gtk.Menu): Контекстное меню, связанное с элементом.
* **Ключевые методы:**
  * `get_preferred_icon_pixbuf(size: int | None = None, resize_method: Literal["hyper", "bilinear", "nearest", "tiles"] | GdkPixbuf.InterpType = GdkPixbuf.InterpType.BILINEAR) -> GdkPixbuf.Pixbuf | None`: Запрашивает Pixbuf иконки этого элемента на основе предпочтительного размера.
  * `context_menu()`: Открывает контекстное меню на стороне сервера.
  * `activate()`: Выполняет основное действие.
  * `secondary_activate()`: Выполняет вторичное действие.
  * `invoke_menu_for_event()`: Вызывает меню для события.
  * `scroll(delta, orientation)`: Обрабатывает прокрутку колеса мыши над элементом.
  * `context_menu_for_event()`: Открывает контекстное меню для события.
  * `activate_for_event()`: Выполняет основное действие для события.
  * `secondary_activate_for_event()`: Выполняет вторичное действие для события.
  * `scroll_for_event()`: Обрабатывает прокрутку колеса мыши для события.
* **Ключевые сигналы:**
  * `changed`: Излучается при любом изменении свойств элемента.
  * `removed`: Излучается при удалении элемента.

### IV. `SystemTray` (Сервер)

Основной сервис, который прослушивает D-Bus и управляет всеми элементами системного трея.

* **Наследуется от:** `Service`
* **Ключевые свойства:**
  * `items` (dict[str, SystemTrayItem]): Словарь, где ключи — это идентификаторы, а значения — объекты `SystemTrayItem`.
* **Ключевые сигналы:**
  * `changed`: Излучается при любом изменении состояния сервера.
  * `item-added`: Излучается при добавлении нового элемента.
  * `item-removed`: Излучается при удалении элемента.
* **Ключевые методы:**
  * `add_item(item)`: Добавляет новый элемент в трей.
  * `remove_item(item)`: Удаляет элемент из трея.

---

## 25. Утилиты (API: Utils)

**URL:** https://wiki.ffpy.org/api/utils/utils/

Модуль `fabric.utils` содержит набор разнообразных вспомогательных классов и функций для работы с системой, файлами, CSS, а также общие утилиты.

### I. Классы

#### `PixbufUtils`

Базовый класс: `object`
Набор статических методов для работы с `GdkPixbuf.Pixbuf`.

* **`from_cairo_surface(surface: ImageSurface, x_offset: int = 0, y_offset: int = 0, target_width: int | None = None, target_height: int | None = None) -> Pixbuf`**
* **`set_cairo_source(pixbuf: Pixbuf, cr: Context, x_offset: int = 0, y_offset: int = 0)`**
* **`rotate(pixbuf: Pixbuf, angle: float) -> Pixbuf`**
  Возвращает повернутую версию `GdkPixbuf.Pixbuf` на заданный угол.
  * **Параметры:** `pixbuf` (GdkPixbuf.Pixbuf) — входной pixbuf, `angle` (float) — желаемый угол поворота в градусах.
  * **Возвращает:** Новый повернутый pixbuf.
  * **Тип возвращаемого значения:** `GdkPixbuf.Pixbuf`

#### `FormattedString`

Базовый класс: `object`
Простой, но мощный форматтер строк, предназначенный для динамического обновления текста во время выполнения.

* **`FormatDict(*args, **kwargs)`**
  Базовый класс: `dict`
* **`format(**kwargs) -> str`**

#### `DesktopApp`

Базовый класс: `object`
Класс-контейнер для информации о `.desktop` файле приложения.

* **`name`** (`str`)
* **`generic_name`** (`str | None`)
* **`display_name`** (`str | None`)
* **`description`** (`str | None`)
* **`window_class`** (`str | None`)
* **`executable`** (`str | None`)
* **`command_line`** (`str | None`)
* **`icon`** (`Icon | ThemedIcon | FileIcon | LoadableIcon | EmblemedIcon | None`)
* **`icon_name`** (`str | None`)
* **`hidden`** (`bool`)
* **`launch() -> None`**
* **`get_icon_pixbuf(size: int = 48, default_icon: str | None = "image-missing", flags: Gtk.IconLookupFlags = (Gtk.IconLookupFlags.FORCE_REGULAR | Gtk.IconLookupFlags.FORCE_SIZE)) -> GdkPixbuf.Pixbuf | None`**

#### `FunctionAnnotations`

Базовый класс: `NamedTuple`, `Generic[T]`
Представляет аннотации функции.

* **`arguments`**: Список аргументов.
* **`return_type`**: Тип возвращаемого значения.

### II. Функции

* **`get_desktop_applications(include_hidden: bool = False) -> list[DesktopApp]`**
  Получает список всех `.desktop` приложений.
* **`parse_color(color: str | Iterable[Number]) -> Gdk.RGBA`**
  Парсит данные цвета в объект `Gdk.RGBA`.
* **`get_gdk_rgba() -> Gdk.RGBA`**
* **`compile_css(css_string: str, base_path: str | None = None, exposed_functions: dict[str, Callable] | Iterable[Callable] | None = None) -> str`**
  Преобразует CSS-строку в синтаксис CSS GTK, поддерживая переменные и макросы.
* **`bulk_replace(string: str, patterns: Iterable[str], replacements: Iterable[str], regex: bool = False) -> str`**
  Заменяет множественные вхождения шаблонов в строке.
* **`bulk_connect(connectable: GObject.Object, mapping: dict[str, Callable]) -> tuple[int, ...]`**
  Подключает список сигналов к списку обратных вызовов для объекта.
* **`bulk_disconnect(disconnectable: GObject.Object | object, signals_or_funcs: Iterable) -> tuple[int]`**
  Отключает сигналы от объекта.
* **`clamp(value: float | int, min_value: float | int, max_value: float | int) -> float | int`**
  Ограничивает значение между минимумом и максимумом.
* **`extract_css_values(css_string: str) -> tuple`**
  Извлекает и возвращает кортеж из четырех значений CSS из данной CSS-строки.
* **`monitor_file(file_path: str, flags: Literal["none", "watch-mounts", "send-moved", "watch-hard-links", "watch-moves"] = None) -> Gio.FileMonitor`**
  Создает монитор для указанного файла или директории.
* **`cooldown(cooldown_time: int, error: Callable | None = None) -> Callable`**
  Декоратор, добавляющий период перезарядки к функции.
* **`exec_shell_command(cmd: str) -> str | Literal[False]`**
  Выполняет команду оболочки и возвращает вывод.
* **`exec_shell_command_async(cmd: str, callback: Callable[[str], Any] | None = None, *args: Any, **kwargs: Any) -> tuple[Gio.Subprocess | None, Gio.DataInputStream]`**
  Выполняет команду оболочки асинхронно.
* **`invoke_repeater(interval: int, func: Callable) -> None`**
  Повторно вызывает функцию через заданный интервал.
* **`get_relative_path(path: str, level: int = 1) -> str`**
  Преобразует путь в относительный путь.
* **`load_dbus_xml()`**
* **`snake_case_to_pascal_case()`**
* **`pascal_case_to_snake_case()`**
* **`snake_case_to_kebab_case()`**
* **`kebab_case_to_snake_case()`**
* **`get_enum_member()`**
* **`get_enum_member_name()`**
* **`bridge_signal()`**
* **`generate_random_string()`**
* **`get_function_annotations()`**
* **`truncate()`**
* **`idle_add(func: Callable, args: Any, pin: bool = False) -> None`**
  Добавляет функцию в очередь для вызова в основном потоке.
* **`remove_handler()`**
* **`get_connectables_for_kwargs()`** (Устарело, будет удалено в будущих версиях Fabric.)
* **`set_stylesheet_from_file()`** (Устарело, используйте `Application.add_stylesheet_from_file`.)
* **`set_stylesheet_from_string()`** (Устарело, используйте `Application.add_stylesheet_from_string`.)
* **`idlify()`** (Устарело, используйте `idle_add`.)
* **`icon_name`** (`str | None`)
* **`hidden`** (`bool`)
* **`launch()`**
* **`get_icon_pixbuf(size: int = 48, default_icon: str | None = 'image-missing', flags: IconLookupFlags = Gtk.IconLookupFlags.FORCE_REGULAR | Gtk.IconLookupFlags.FORCE_SIZE) -> Pixbuf | None`**
  Получает pixbuf из иконки (если есть).
  * **Параметры:** `size` (int, опционально) — размер иконки (по умолчанию 48), `default_icon` (str | None, опционально) — имя иконки по умолчанию (по умолчанию 'image-missing'), `flags` (Gtk.IconLookupFlags, опционально) — флаги для получения иконки.
  * **Возвращает:** Pixbuf.
  * **Тип возвращаемого значения:** `GdkPixbuf.Pixbuf | None`

#### `FunctionAnnotations(arguments, return_type)`

Базовый класс: `NamedTuple`, `Generic[T]`
Создает новый экземпляр FunctionAnnotations(arguments, return_type).

* **`arguments`** (`dict[str, type]`)
* **`return_type`** (`T | None`)

### II. Функции

#### Работа с системой и файлами:

* **`get_desktop_applications(include_hidden: bool = False) -> list[DesktopApp]`**
  Возвращает список всех `.desktop` приложений.
  * **Параметры:** `include_hidden` (bool, опционально) — включать ли скрытые приложения (по умолчанию False).
  * **Возвращает:** Список всех `.desktop` приложений.
  * **Тип возвращаемого значения:** `list[DesktopApp]`
* **`monitor_file(file_path: str, flags: Literal['none', 'watch-mounts', 'send-moved', 'watch-hard-links', 'watch-moves'] | FileMonitorFlags = Gio.FileMonitorFlags.NONE) -> FileMonitor`**
  Создает монитор для указанного файла или директории.
  * **Параметры:** `file_path` (str), `flags` (Literal, опционально) — флаги для настройки монитора файлов.
  * **Возвращает:** Монитор для указанного пути.
  * **Тип возвращаемого значения:** `Gio.FileMonitor`
* **`exec_shell_command(cmd: str) -> str | Literal[False]`**
  Выполняет команду оболочки и возвращает вывод.
  * **Параметры:** `cmd` (str) — команда оболочки.
  * **Возвращает:** Вывод команды или False, если произошла ошибка.
  * **Тип возвращаемого значения:** `str | Literal[False]`
* **`exec_shell_command_async(cmd: str | list[str], callback: Callable[[str], Any] | None = None) -> tuple[Subprocess | None, DataInputStream]`**
  Выполняет команду оболочки асинхронно и возвращает вывод.
  * **Параметры:** `cmd` (str | list[str]), `callback` (Callable[[str], Any] | None, опционально) — функция для получения результата.
  * **Возвращает:** Объект `Gio.Subprocess` и объект `Gio.DataInputStream` для stdout.
  * **Тип возвращаемого значения:** `tuple[Gio.Subprocess | None, Gio.DataInputStream]`
* **`get_relative_path(path: str, level: int = 1) -> str`**
  Преобразует путь в относительный путь.
  * **Параметры:** `path` (str), `level` (int, опционально) — уровень стека для получения переменной `__file__`.
  * **Возвращает:** Относительный путь.
  * **Тип возвращаемого значения:** `str`

#### Работа с CSS:

* **`compile_css(css_string: str, base_path: str = '.', exposed_functions: dict[str, Callable] | Iterable[Callable] | None = None) -> str`**
  Предварительно обрабатывает и транспилирует строку CSS в синтаксис CSS GTK. Поддерживает переменные, макросы и вызов функций Python.
  * **Параметры:** `css_string` (str), `base_path` (str, опционально) — для операторов `@import`, `exposed_functions` (dict[str, Callable] | Iterable[Callable] | None, опционально) — функции макросов.
  * **Возвращает:** Скомпилированная строка CSS.
  * **Тип возвращаемого значения:** `str`
* **`parse_color(color: str | Iterable[int | float]) -> RGBA`**
  Преобразует сериализованные данные цвета в объект `Gdk.RGBA`.
  * **Параметры:** `color` (str | Iterable[int | float]) — данные цвета.
  * **Вызывает:** `ValueError`, если данные цвета не поддаются разбору.
  * **Возвращает:** Вновь созданный `Gdk.RGBA`.
  * **Тип возвращаемого значения:** `Gdk.RGBA`
* **`get_gdk_rgba(color: str | Iterable[int | float]) -> RGBA`**
* **`extract_css_values(css_string: str) -> tuple[int, int, int, int]`**
  Извлекает и возвращает кортеж из четырех значений CSS из заданной строки CSS.
  * **Параметры:** `css_string` (str) — строка CSS.
  * **Возвращает:** Кортеж из четырех целых чисел, представляющих извлеченные значения CSS.
  * **Тип возвращаемого значения:** `tuple`

#### Общие утилиты:

* **`truncate(string: str, max_length: int, suffix: str = '...') -> str`**
  Обрезает строку до максимальной длины, добавляя многоточие.
  * **Параметры:** `string` (str), `max_length` (int), `suffix` (str, опционально) — суффикс (по умолчанию '...').
  * **Возвращает:** Обрезанная строка.
  * **Тип возвращаемого значения:** `str`
* **`clamp(value: int | float, min_value: int | float, max_value: int | float) -> int | float`**
  Ограничивает значение между минимальным и максимальным значениями.
  * **Параметры:** `value` (float или int), `min_value` (float или int), `max_value` (float или int).
  * **Возвращает:** Ограниченное значение.
  * **Тип возвращаемого значения:** `float` или `int`
* **`bulk_replace(string: str, patterns: Iterable[str], replacements: Iterable[str], regex: bool = False) -> str`**
  Заменяет вхождения нескольких шаблонов в строке соответствующими заменами.
  * **Параметры:** `string` (str), `patterns` (Iterable[str]), `replacements` (Iterable[str]), `regex` (bool, опционально).
  * **Возвращает:** Строка с произведенными заменами.
  * **Тип возвращаемого значения:** `str`
  * **Вызывает:** `ValueError`, если длины шаблонов и замен не совпадают.
* **`bulk_connect(connectable: Object, mapping: dict[str, Callable]) -> tuple[int, ...]`**
  Подключает список сигналов к списку обратных вызовов объекта.
  * **Параметры:** `connectable` (GObject.Object), `mapping` (dict[str, Callable]).
  * **Тип возвращаемого значения:** `tuple[int, ...]`
* **`bulk_disconnect(disconnectable: Object, signals_or_funcs: Iterable[str | Callable]) -> tuple[int, ...]`**
  Делает противоположное `bulk_connect`.
  * **Параметры:** `disconnectable` (GObject.Object | object), `signals_or_funcs` (Iterable[str | Callable]) — сигналы/обратные вызовы для отключения.
  * **Возвращает:** Список возвращаемых значений из функции отключения.
  * **Тип возвращаемого значения:** `tuple[int]`
* **`idle_add(func: Callable, *args, pin: bool = False) -> int`**
  Добавляет функцию для асинхронного вызова в основном потоке, полезно для многопоточного кода.
  * **Параметры:** `func` (Callable), `args`, `pin` (bool, опционально).
* **`cooldown(cooldown_time: int, error: Callable | None = None, return_error: bool = False)`**
  Функция-декоратор, которая добавляет период задержки к заданной функции.
  * **Параметры:** `cooldown_time` (int) — время в секундах, `error` (Callable | None, опционально) — функция для вызова, если период задержки не был достигнут.
  * **Тип возвращаемого значения:** `decorator`
* **`invoke_repeater(interval: int, func: Callable, *args, initial_call: bool = True) -> int`**
  Повторно вызывает функцию с заданным интервалом.
  * **Параметры:** `interval` (int) — интервал в миллисекундах, `func` (Callable) — функция для вызова.
* **`load_dbus_xml(path_to_xml: str)`**
* **`snake_case_to_pascal_case(string: str) -> str`**
* **`pascal_case_to_snake_case(string: str) -> str`**
* **`snake_case_to_kebab_case(string: str) -> str`**
* **`kebab_case_to_snake_case(string: str) -> str`**
* **`get_enum_member(enum: type[E], member: str | E, mapping: dict[str, str] = {}, default: Any = MISSING) -> E`**
* **`get_enum_member_name(member: Enum | Any, mapping: dict[str, str] = {}, default: Any = MISSING) -> str`**
* **`bridge_signal(source: Object, source_signal: str, target: Object, target_signal: str, notify: bool = False) -> int`**
* **`generate_random_string(length: int = 8) -> str`**
* **`get_function_annotations(func: Callable[[P], T]) -> FunctionAnnotations[T]`**
* **`remove_handler(handler_id: int)`**

#### Устаревшие функции:

* **`get_connectables_for_kwargs`**
  Устаревшая функция, будет удалена в будущих версиях Fabric.
* **`set_stylesheet_from_file(file_path: str, compiled: bool = True) -> None`**
  Устаревшая функция. Рассмотрите использование `Application.add_stylesheet_from_file` вместо нее.
* **`set_stylesheet_from_string(css_string: str, compiled: bool = True) -> None`**
  Устаревшая функция. Рассмотрите использование `Application.add_stylesheet_from_string` вместо нее.
* **`idlify(func: Callable, *args) -> int`**
  Устаревшая функция. Рассмотрите использование `idle_add` вместо нее.

---

## 26. Box (Контейнер)

**URL:** https://wiki.ffpy.org/api/widgets/box/

### I. Общее описание

**Box** — это контейнер, способный удерживать виджеты. Наследует от `Gtk.Box` и `Container`.

### II. Параметры конструктора

*   **spacing** (`int`, по умолчанию `0`): Расстояние между дочерними виджетами в пикселях.
*   **orientation** (`Literal['v', 'vertical', 'h', 'horizontal'] | Orientation`, по умолчанию `Gtk.Orientation.HORIZONTAL`): Ориентация контейнера.
*   **children** (`Widget | Iterable[Widget] | None`, по умолчанию `None`): Один или несколько виджетов для добавления.
*   **name** (`str | None`, по умолчанию `None`): Идентификатор виджета (для стилизации).
*   **visible** (`bool`, по умолчанию `True`): Должен ли виджет быть видимым после инициализации.
*   **all_visible** (`bool`, по умолчанию `False`): Должен ли виджет и все его потомки быть видимыми.
*   **style** (`str | None`, по умолчанию `None`): Встроенный CSS-стиль.
*   **style_classes** (`Iterable[str] | str | None`, по умолчанию `None`): Список CSS-классов.
*   **tooltip_text** (`str | None`, по умолчанию `None`): Текст всплывающей подсказки.
*   **tooltip_markup** (`str | None`, по умолчанию `None`): Pango-разметка для подсказки.
*   **h_align** (`Literal['fill', 'start', 'end', 'center', 'baseline'] | Gtk.Align | None`, по умолчанию `None`): Горизонтальное выравнивание.
*   **v_align** (`Literal['fill', 'start', 'end', 'center', 'baseline'] | Gtk.Align | None`, по умолчанию `None`): Вертикальное выравнивание.
*   **h_expand** (`bool`, по умолчанию `False`): Заполнять ли всё доступное горизонтальное пространство.
*   **v_expand** (`bool`, по умолчанию `False`): Заполнять ли всё доступное вертикальное пространство.
*   **size** (`Iterable[int] | int | None`, по умолчанию `None`): Фиксированный размер (не гарантируется применение).

---

## 27. Button (Кнопка)

**URL:** https://wiki.ffpy.org/api/widgets/button/

### I. Общее описание

**Button** — нажимаемая кнопка, которая вызывает функцию обратного вызова при нажатии. Наследует от `Gtk.Button` и `Container`.

### II. Параметры конструктора

*   **label** (`str | None`, по умолчанию `None`): Текст метки, используемой как дочерний элемент кнопки.
*   **image** (`Gtk.Image | None`, по умолчанию `None`): Виджет изображения внутри кнопки.
*   **child** (`Gtk.Widget | None`, по умолчанию `None`): Произвольный дочерний виджет.
*   **name**, **visible**, **all_visible**, **style**, **style_classes**, **tooltip_text**, **tooltip_markup**, **h_align**, **v_align**, **h_expand**, **v_expand**, **size**: Стандартные параметры виджета.

### III. Важное примечание

Рекомендуется не использовать другие опциональные дочерние элементы (метку или изображение) при установке параметра `child`.

---

## 28. CenterBox (Центрированный контейнер)

**URL:** https://wiki.ffpy.org/api/widgets/centerbox/

### I. Общее описание

Контейнер с тремя секциями (начало, центр, конец), где центральная секция всегда остается посередине. Наследует от `Box`.

### II. Параметры конструктора

*   **start_children** (`Widget | Iterable[Widget] | None`, по умолчанию `None`): Виджет(ы) для начальной секции.
*   **center_children** (`Widget | Iterable[Widget] | None`, по умолчанию `None`): Виджет(ы) для центральной секции.
*   **end_children** (`Widget | Iterable[Widget] | None`, по умолчанию `None`): Виджет(ы) для конечной секции.
*   **spacing**, **orientation**, **name**, **visible**, **all_visible**, **style**, **style_classes**, **tooltip_text**, **tooltip_markup**, **h_align**, **v_align**, **h_expand**, **v_expand**, **size**: Стандартные параметры.

### III. Свойства

*   **start_children**, **center_children**, **end_children** (`list[Widget]`): Списки виджетов в соответствующих секциях.

### IV. Методы

*   **add_start(widget)**, **add_center(widget)**, **add_end(widget)**: Добавление виджетов.
*   **remove_start(widget)**, **remove_center(widget)**, **remove_end(widget)**: Удаление виджетов.

---

## 29. CircularProgressBar (Круговой индикатор)

**URL:** https://wiki.ffpy.org/api/widgets/circularprogressbar/

### I. Общее описание

Виджет для отображения прогресса в круговой форме. Наследует от `DrawingArea` и `Widget`.

### II. Параметры конструктора

*   **value** (`float`, по умолчанию `1.0`): Начальное значение прогресса.
*   **min_value** (`float`, по умолчанию `0.0`): Минимальное значение.
*   **max_value** (`float`, по умолчанию `1.0`): Максимальное значение.
*   **line_width** (`int`, по умолчанию `4`): Толщина линии (в пикселях). Можно задать через CSS `border-width`.
*   **line_style** (`Literal['none', 'butt', 'round', 'square'] | cairo.LineCap`, по умолчанию `cairo.LineCap.ROUND`): Стиль концов линии.
*   **pie** (`bool`, по умолчанию `False`): Рисовать ли прогресс-бар в форме "пирога".
*   **name**, **visible**, **all_visible**, **style**, **style_classes**, **tooltip_text**, **tooltip_markup**, **h_align**, **v_align**, **h_expand**, **v_expand**, **size**: Стандартные параметры.

### III. Свойства

*   **value** (`float`): Текущее значение.
*   **min_value** (`float`): Минимальное значение.
*   **max_value** (`float`): Максимальное значение.
*   **line_width** (`int`): Ширина заполнения (в пикселях).
*   **line_style** (`cairo.LineCap`): Форма концов линии (butt, round, square).
*   **pie** (`bool`): Отображение в виде сектора.

---

## 30. Container (Базовый контейнер)

**URL:** https://wiki.ffpy.org/api/widgets/container/

### I. Общее описание

Базовый класс для контейнеров Fabric. Наследует от `Gtk.Container` и `Widget`.

### II. Параметры конструктора

*   **children** (`Widget | Iterable[Widget] | None`, по умолчанию `None`): Дочерние виджеты.
*   **name**, **visible**, **all_visible**, **style**, **style_classes**, **tooltip_text**, **tooltip_markup**, **h_align**, **v_align**, **h_expand**, **v_expand**, **size**: Стандартные параметры.

### III. Свойства

*   **children** (`list[Gtk.Widget]`): Список детей, которые (в данный момент) содержит контейнер.

---

## 31. DateTime (Дата и время)

**URL:** https://wiki.ffpy.org/api/widgets/datetime/

### I. Общее описание

Простая реализация виджета даты и времени. Представляет собой кнопку, которая при клике переключается на следующий формат из списка. Наследует от `Button`.

### II. Параметры конструктора

*   **formatters** (`str | Iterable[str]`, по умолчанию `('%I:%M %p', '%A', '%m-%d-%Y')`): Строка формата (или список строк) для отображения.
*   **interval** (`int`, по умолчанию `1000`): Интервал обновления содержимого в миллисекундах.
*   **name**, **visible**, **all_visible**, **style**, **style_classes**, **tooltip_text**, **tooltip_markup**, **h_align**, **v_align**, **h_expand**, **v_expand**, **size**: Стандартные параметры.

---

## 32. Entry (Поле ввода)

**URL:** https://wiki.ffpy.org/api/widgets/entry/

### I. Общее описание

Простое поле текстового ввода. Обычно используется для ввода одной строки текста. Наследует от `Gtk.Entry` и `Widget`.

### II. Параметры конструктора

*   **text** (`str | None`, по умолчанию `None`): Начальный текст.
*   **placeholder** (`str | None`, по умолчанию `None`): Текст-подсказка, когда поле пустое.
*   **editable** (`bool`, по умолчанию `True`): Может ли пользователь редактировать текст.
*   **password** (`bool`, по умолчанию `False`): Скрывать ли вводимые символы (режим пароля).
*   **max_length** (`int`, по умолчанию `0`): Максимальное количество символов (0 — без ограничений).
*   **name**, **visible**, **all_visible**, **style**, **style_classes**, **tooltip_text**, **tooltip_markup**, **h_align**, **v_align**, **h_expand**, **v_expand**, **size**: Стандартные параметры.

---

## 33. EventBox (Перехватчик событий)

**URL:** https://wiki.ffpy.org/api/widgets/eventbox/

### I. Общее описание

Простой контейнер для захвата определенного набора событий. Полезен для добавления интерактивности виджетам, которые обычно не обрабатывают события. Наследует от `Gtk.EventBox` и `Container`.

### II. Параметры конструктора

*   **events** (`Literal[...] | Gdk.EventMask | Iterable[...] | None`, по умолчанию `None`): Событие или список событий, которые может получать этот виджет (напр., 'button-press', 'scroll', 'pointer-motion').
*   **child** (`Widget | None`, по умолчанию `None`): Дочерний виджет.
*   **name**, **visible**, **all_visible**, **style**, **style_classes**, **tooltip_text**, **tooltip_markup**, **h_align**, **v_align**, **h_expand**, **v_expand**, **size**: Стандартные параметры.

---

## 34. Fixed (Контейнер с ручным позиционированием)

**URL:** https://wiki.ffpy.org/api/widgets/fixed/

### I. Общее описание

Контейнер, который не управляет компоновкой своих дочерних элементов автоматически. Дети должны быть позиционированы явно по координатам X и Y. Наследует от `Gtk.Fixed` и `Widget`.

### II. Параметры конструктора

*   **children** (`Iterable[tuple[Widget, tuple[int, int]]] | None`, по умолчанию `None`): Список кортежей, где каждый кортеж содержит виджет и еще один кортеж с его координатами (x, y).
*   **name**, **visible**, **all_visible**, **style**, **style_classes**, **tooltip_text**, **tooltip_markup**, **h_align**, **v_align**, **h_expand**, **v_expand**, **size**: Стандартные параметры.

### III. Методы

*   **put(widget, x, y)**: Добавление виджета в указанную позицию.

---

## 35. FlowBox (Автоматическая сетка)

**URL:** https://wiki.ffpy.org/api/widgets/flowbox/

### I. Общее описание

Контейнер, который располагает дочерние виджеты в сетку, перенося их на следующую строку/столбец при необходимости. Наследует от `Gtk.FlowBox` и `Container`.

### II. Параметры конструктора

*   **row_spacing** (`int`, по умолчанию `0`): Расстояние между строками.
*   **column_spacing** (`int`, по умолчанию `0`): Расстояние между столбцами.
*   **orientation** (`Literal['v', 'vertical', 'h', 'horizontal'] | Orientation`, по умолчанию `Gtk.Orientation.HORIZONTAL`): Направление заполнения.
*   **children** (`Widget | Iterable[Widget] | None`, по умолчанию `None`): Дочерние виджеты.
*   **name**, **visible**, **all_visible**, **style**, **style_classes**, **tooltip_text**, **tooltip_markup**, **h_align**, **v_align**, **h_expand**, **v_expand**, **size**: Стандартные параметры.

---

## 36. Hyprland (Интеграция с Hyprland)

**URL:** https://wiki.ffpy.org/api/widgets/hyprland/

### I. Функции

*   **get_hyprland_connection()**: Возвращает объект сервиса `Hyprland`.

### II. WorkspaceButton (Кнопка рабочего стола)

Специализированная кнопка для использования с виджетом `Workspaces`. Наследует от `Button`.

*   **Параметры:**
    - **id** (`int`): Идентификатор рабочего стола.
    - **label** (`FormattedString | str | None`): Метка кнопки.
    - **image** (`Image | None`): Изображение.
*   **Свойства:**
    - **id** (`int`): ID рабочего стола.
    - **active** (`bool`): Является ли текущим активным столом.
    - **urgent** (`bool`): Помечен ли стол как срочный.
    - **empty** (`bool`): Помечен ли стол как пустой.

### III. Workspaces (Рабочие столы)

Виджет для отображения и управления рабочими столами Hyprland. Наследует от `EventBox`.

*   **Параметры:**
    - **buttons** (`Iterable[WorkspaceButton] | None`): Список предопределенных кнопок.
    - **buttons_factory** (`Callable[[int], WorkspaceButton | None]`): Фабрика для динамического создания кнопок. По умолчанию `default_buttons_factory`.
    - **invert_scroll** (`bool`): Инвертировать направление прокрутки колесом мыши.
    - **empty_scroll** (`bool`): Прокручивать ли пустые столы.
*   **Методы:** `insert_button(button)`, `reorder_buttons()`, `remove_button(button)`, `lookup_or_bake_button(workspace_id)`.

### IV. ActiveWindow (Активное окно)

Отображает информацию о текущем сфокусированном окне. Наследует от `Button`.

*   **Параметры:**
    - **formatter** (`FormattedString`): Шаблон строки. По умолчанию: `"{'Desktop' if not win_title else truncate(win_title, 42)}"`.

### V. Language (Язык)

Отображает текущую раскладку клавиатуры. Наследует от `Button`.

*   **Параметры:**
    - **keyboard** (`str`): Регулярное выражение для выбора устройства клавиатуры. По умолчанию `'.*'`.
    - **formatter** (`FormattedString`): Шаблон строки. По умолчанию `"{language}"`.

---

## 37. Image (Изображение)

**URL:** https://wiki.ffpy.org/api/widgets/image/

### I. Общее описание

Виджет для отображения изображений. Наследует от `Gtk.Image` и `Widget`.

### II. Параметры конструктора

Виджет поддерживает несколько вариантов инициализации:
1. **Через файл:** `image_file` (`str | None`) — путь к файлу.
2. **Через системную иконку:** `icon_name` (`str | None`) — имя иконки.
3. **Через Pixbuf:** `pixbuf` (`GdkPixbuf.Pixbuf | None`).

*   **icon_size** (`int`, по умолчанию `24`): Размер иконки (в пикселях).
*   **name**, **visible**, **all_visible**, **style**, **style_classes**, **tooltip_text**, **tooltip_markup**, **h_align**, **v_align**, **h_expand**, **v_expand**, **size**: Стандартные параметры.

### III. Методы

*   **set_from_icon_name(icon_name, size)**: Обновить иконку и её размер.

---

## 38. Label (Текстовая метка)

**URL:** https://wiki.ffpy.org/api/widgets/label/

### I. Общее описание

Виджет для отображения текста. Наследует от `Gtk.Label` и `Widget`.

### II. Параметры конструктора

1. **Обычный текст:** `label` (`str | None`).
2. **Pango-разметка:** `markup` (`str | None`).

*   **justification** (`Literal['left', 'right', 'center', 'fill'] | Justification`, по умолчанию `LEFT`): Выравнивание текста.
*   **ellipsization** (`Literal['none', 'start', 'middle', 'end'] | EllipsizeMode`, по умолчанию `NONE`): Способ сокращения длинного текста.
*   **chars_width** (`int`, по умолчанию `-1`): Ширина в символах.
*   **max_chars_width** (`int`, по умолчанию `-1`): Максимальная ширина в символах.
*   **line_wrap** (`Literal['word', 'char', 'word-char'] | WrapMode | None`, по умолчанию `None`): Перенос строк.
*   **angle** (`float`, по умолчанию `0.0`): Угол поворота текста.
*   **name**, **visible**, **all_visible**, **style**, **style_classes**, **tooltip_text**, **tooltip_markup**, **h_align**, **v_align**, **h_expand**, **v_expand**, **size**: Стандартные параметры.

---

## 39. Overlay (Наложение слоев)

**URL:** https://wiki.ffpy.org/api/widgets/overlay/

### I. Общее описание

Контейнер, позволяющий накладывать виджеты друг на друга. Наследует от `Gtk.Overlay` и `Container`.

### II. Параметры конструктора

*   **child** (`Widget | None`, по умолчанию `None`): Основной (нижний) виджет.
*   **overlays** (`Widget | list[Widget] | None`, по умолчанию `None`): Список виджетов, накладываемых поверх основного.

### III. Свойства

*   **overlays** (`list[Widget]`): Список текущих наложенных элементов.

### IV. Методы

*   **add_overlay(widget)**: Добавить новый слой поверх существующих.
*   **remove_overlay(overlay)**: Удалить указанный слой.

---

## 40. Revealer (Анимированное появление)

**URL:** https://wiki.ffpy.org/api/widgets/revealer/

### I. Общее описание

Контейнер, который плавно скрывает или показывает своего потомка. Наследует от `Gtk.Revealer` и `Container`.

### II. Параметры конструктора

*   **child** (`Widget | None`, по умолчанию `None`): Дочерний виджет.
*   **child_revealed** (`bool`, по умолчанию `False`): Состояние видимости (показан или скрыт).
*   **transition_type** (`Literal['none', 'crossfade', 'slide-right', 'slide-left', 'slide-up', 'slide-down'] | RevealerTransitionType`, по умолчанию `NONE`).
*   **transition_duration** (`int`, по умолчанию `400`): Длительность анимации в мс.

### III. Свойства

*   **fully_revealed** (`bool`): Полностью ли завершена анимация появления.
*   **child_revealed** (`bool`): Текущее целевое состояние видимости.
*   **transition_type** (`RevealerTransitionType`).
*   **transition_duration** (`int`).

### IV. Методы

*   **reveal()**: Показать дочерний виджет.
*   **unreveal()**: Скрыть дочерний виджет.

---

## 41. Scale (Ползунок)

**URL:** https://wiki.ffpy.org/api/widgets/scale/

### I. Общее описание

Виджет-ползунок для выбора числового значения. Наследует от `Gtk.Scale` и `Widget`.

### II. ScaleMark (Метка шкалы)

Класс (NamedTuple) для создания делений на шкале: `ScaleMark(value, markup, position)`.
*   **value** (`float`): Значение на шкале.
*   **markup** (`str | None`): Текст (с разметкой) рядом с делением.
*   **position** (`Literal['bottom', 'left', 'right', 'top'] | PositionType`): Положение текста.

### III. Параметры конструктора

*   **value** (`float`, по умолчанию `0.0`): Текущее значение.
*   **min_value** (`float`, по умолчанию `0.0`): Минимальное значение.
*   **max_value** (`float`, по умолчанию `1.0`): Максимальное значение.
*   **draw_value** (`bool`, по умолчанию `False`): Рисовать ли числовое значение.
*   **value_position** (`Literal['bottom', 'left', 'right', 'top'] | PositionType`, по умолчанию `TOP`).
*   **orientation** (`Literal['v', 'vertical', 'h', 'horizontal'] | Orientation`, по умолчанию `HORIZONTAL`).
*   **increments** (`tuple[float, float] | None`, по умолчанию `None`): Шаги изменения.
*   **marks** (`Iterable[ScaleMark] | None`, по умолчанию `None`): Список делений.
*   **digits** (`int`, по умолчанию `1`): Количество знаков после запятой.
*   **inverted** (`bool`, по умолчанию `False`): Инвертировать направление.
*   **has_origin** (`bool`, по умолчанию `True`).

---

## 42. ScrolledWindow (Прокручиваемая область)

**URL:** https://wiki.ffpy.org/api/widgets/scrolledwindow/

### I. Общее описание

Контейнер с полосами прокрутки для отображения контента, превышающего размер окна. Наследует от `Gtk.ScrolledWindow` и `Container`.

### II. Параметры конструктора

*   **min_content_size** (`Iterable[int]`, по умолчанию `(-1, -1)`).
*   **max_content_size** (`Iterable[int]`, по умолчанию `(-1, -1)`).
*   **propagate_width** (`bool`, по умолчанию `True`).
*   **propagate_height** (`bool`, по умолчанию `True`).
*   **kinetic_scroll** (`bool`, по умолчанию `False`).
*   **overlay_scroll** (`bool`, по умолчанию `False`).
*   **h_scrollbar_policy** (`Literal['always', 'automatic', 'never', 'external'] | PolicyType`, по умолчанию `AUTOMATIC`).
*   **v_scrollbar_policy** (`Literal['always', 'automatic', 'never', 'external'] | PolicyType`, по умолчанию `AUTOMATIC`).
*   **child** (`Widget | None`, по умолчанию `None`): Дочерний виджет.

---

## 43. Corner (Скругленный угол)

**URL:** https://wiki.ffpy.org/api/widgets/shapes/corner/

### I. Общее описание

Виджет для отрисовки скругленного угла. Наследует от `DrawingArea` и `Widget`.

### II. CornerOrientation (Перечисление)

`TOP_LEFT` (1), `TOP_RIGHT` (2), `BOTTOM_LEFT` (3), `BOTTOM_RIGHT` (4).

### III. Параметры конструктора

*   **orientation** (`Literal['top-left', 'top-right', 'bottom-left', 'bottom-right'] | CornerOrientation`, по умолчанию `TOP_RIGHT`).

### IV. Методы

*   **render_shape(cr, width, height, orientation)**: Статический метод для отрисовки фигуры в контексте Cairo.

---

## 44. Star (Звезда)

**URL:** https://wiki.ffpy.org/api/widgets/shapes/star/

### I. Общее описание

Виджет для отрисовки звезды. Наследует от `DrawingArea` и `Widget`.

### II. Параметры конструктора

*   **points** (`int`, по умолчанию `5`): Количество лучей.
*   **ratio** (`float`, по умолчанию `0.5`): Отношение внутреннего радиуса к внешнему (острота лучей).

### III. Методы

*   **render_shape(cr, width, height, points, ratio)**: Статический метод отрисовки.

---

## 45. Stack (Контейнер-стек)

**URL:** https://wiki.ffpy.org/api/widgets/stack/

### I. Общее описание

Контейнер, который показывает только один из своих дочерних виджетов одновременно. Наследует от `Gtk.Stack` и `Container`.

### II. Параметры конструктора

*   **transition_type** (`Literal['none', 'crossfade', 'slide-right', 'slide-left', 'slide-up', 'slide-down', ...]` | `StackTransitionType`, по умолчанию `NONE`).
*   **transition_duration** (`int`, по умолчанию `400`): Длительность анимации перехода в мс.
*   **children** (`Widget | Iterable[Widget] | None`, по умолчанию `None`).

---

## 46. System Tray (Системный трей)

**URL:** https://wiki.ffpy.org/api/widgets/system_tray/

### I. Общее описание

Виджет для отображения системного трея. Наследует от `Box`.

### II. SystemTray

*   **icon_size** (`int`, по умолчанию `24`): Размер значков в трее.

### III. SystemTrayItem

Виджет отдельного элемента трея. Наследует от `Button`.
*   **item** (`SystemTrayItem`): Объект сервиса SystemTrayItem.
*   **icon_size** (`int`): Размер иконки.

---

## 47. Wayland (Окно Wayland Layer)

**URL:** https://wiki.ffpy.org/api/widgets/wayland/

### I. Общее описание

Специфичное для Wayland окно для целей докинга (работает как слой). Наследует от `Window`.

### II. WaylandWindowExclusivity (Перечисление)

*   **NONE** (1): Не резервировать место.
*   **NORMAL** (2): Резервировать место.
*   **AUTO** (3): Автоматически определять необходимость резервирования.

### III. Параметры конструктора

*   **layer** (`Literal['background', 'bottom', 'top', 'overlay'] | Layer`, по умолчанию `TOP`).
*   **anchor** (`str`, по умолчанию `''`): Края привязки (напр., "top right bottom").
*   **margin** (`str | Iterable[int]`, по умолчанию `'0px 0px 0px 0px'`).
*   **exclusivity** (`Literal['none', 'normal', 'auto'] | WaylandWindowExclusivity`, по умолчанию `NONE`).
*   **keyboard_mode** (`Literal['none', 'exclusive', 'on-demand'] | KeyboardMode`, по умолчанию `NONE`).
*   **pass_through** (`bool`, по умолчанию `False`): Пропускать ли события мыши сквозь окно.
*   **monitor** (`int | Gdk.Monitor | None`, по умолчанию `None`).
*   **title** (`str`, по умолчанию `'fabric'`).

### IV. Методы

*   **steal_input()**: Запросить у композитора фокус клавиатуры.
*   **return_input()**: Вернуть фокус клавиатуры.
*   **extract_anchor_values(string)**, **extract_edges_from_string(string)**, **extract_margin(input)**: Статические утилиты для парсинга геометрии.

---

## 48. WebView (Браузерный виджет)

**URL:** https://wiki.ffpy.org/api/widgets/webview/

### I. Общее описание

Виджет для загрузки веб-страниц (локальных и удаленных). Предоставляет "мост" для связи между Python и JavaScript. Наследует от `WebView` и `Widget`.

### II. Параметры конструктора

*   **url** (`str | None`, по умолчанию `None`): URL страницы.
*   **html** (`str | None`, по умолчанию `None`): Прямой HTML-код.
*   **open_bridge** (`bool`, по умолчанию `True`): Включить мост в JavaScript.
*   **open_inspector** (`bool`, по умолчанию `False`): Включить инструменты разработчика.

### III. Мост (Bridge)

Объект для взаимодействия доступен через поле `.bridge` (если `open_bridge=True`).
*   **bridge.expose_function(name, func)**: Проброс функции Python в JavaScript.
*   **Предупреждение:** Использование моста с удаленными страницами не рекомендуется из соображений безопасности.

---

## 49. Widget (Базовый класс виджета)

**URL:** https://wiki.ffpy.org/api/widgets/widget/

### I. Общее описание

Базовый виджет, от которого должны наследоваться все остальные виджеты Fabric. Наследует от `Gtk.Widget` и `Service`.

### II. Свойства

*   **style_classes** (`list[str]`): Список текущих CSS-классов.
*   **v_align**, **h_align** (`Gtk.Align`): Выравнивание.
*   **v_expand**, **h_expand** (`bool`): Расширение.

### III. Методы

*   **set_alignment(orientation, alignment)**: Установка выравнивания для заданной ориентации.
*   **set_style(style, compile=True, append=False, add_brackets=True)**: Установка CSS из строки.
*   **set_cursor(cursor, ...)**: Установка курсора мыши.
*   **is_hovered(event=None)**: Наведен ли курсор на виджет.
*   **add_style_class(class_name)**, **remove_style_class(class_name)**.
*   **add_events(events)**: Регистрация новых типов событий Gdk для обработки виджетом.

---

## 50. Window (Окно верхнего уровня)

**URL:** https://wiki.ffpy.org/api/widgets/window/

### I. Общее описание

Базовое окно верхнего уровня. Наследует от `Gtk.Window` и `Container`.

### II. Параметры конструктора

*   **title** (`str`, по умолчанию `'fabric'`): Заголовок окна.
*   **type** (`Literal['top-level', 'popup'] | Gtk.WindowType`, по умолчанию `TOPLEVEL`).
*   **child** (`Widget | None`, по умолчанию `None`): Основной контент.

### III. Методы

*   **add_keybinding(keybind, callback)**: Добавить обработчик горячей клавиши (напр., "Ctrl w"). Возвращает ID соединения.
*   **remove_keybinding(reference)**: Удалить обработчик по ID, функции или строке клавиш.

---

## 51. X11 (Окно X11 / XOrg)

**URL:** https://wiki.ffpy.org/api/widgets/x11/

### I. Общее описание

Окно для X11/XOrg с поддержкой докинга. Наследует от `Window`.

### II. Перечисления

*   **X11WindowLayer**: `TOP` (1), `BOTTOM` (2).
*   **X11WindowGeometry**: `CENTER` (1), `CENTER_AUTO` (2), `TOP` (3), `TOP_LEFT` (4), `TOP_RIGHT` (5), `BOTTOM` (6), `BOTTOM_LEFT` (7), `BOTTOM_RIGHT` (8), `LEFT` (9), `RIGHT` (10).

### III. Параметры конструктора

*   **type_hint** (`Gdk.WindowTypeHint`, по умолчанию `DOCK`): Тип окна.
*   **geometry** (`Literal[...] | X11WindowGeometry`, по умолчанию `TOP`).
*   **margin** (`str | Iterable[int]`, по умолчанию `'0px 0px 0px 0px'`).
*   **layer** (`Literal['top', 'bottom'] | X11WindowLayer`, по умолчанию `TOP`).
*   **sticky**, **focusable**, **resizable**, **decorated**, **taskbar_hint**, **pager_hint** (`bool`).

### IV. Методы

*   **steal_input()**, **unsteal_input()**: Захват/освобождение ввода.
*   **steal_input_soft(can_release=False)**.

---

---

## 52. Svg (SVG Изображение)

**URL:** https://wiki.ffpy.org/api/widgets/svg/

### I. Общее описание

Виджет для отрисовки масштабируемой векторной графики (SVG). Наследует от `DrawingArea` и `Widget`.

### II. Параметры конструктора

*   **svg_file** (`str | None`, по умолчанию `None`): Путь к файлу SVG.
*   **svg_string** (`str | None`, по умолчанию `None`): Строка с кодом SVG.
*   **name**, **visible**, **all_visible**, **style**, **style_classes**, **tooltip_text**, **tooltip_markup**, **h_align**, **v_align**, **h_expand**, **v_expand**, **size**: Стандартные параметры виджета.

### III. Методы

*   **set_style(style)**: Установка стиля (`Gtk.Style`).
*   **set_from_file(file)**: Загрузка SVG из файла.
*   **set_from_string(string)**: Загрузка SVG из строки.

---
